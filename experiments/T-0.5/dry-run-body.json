{
  "model": "claude-sonnet-4-6",
  "max_tokens": 600,
  "temperature": 0,
  "system": [
    {
      "type": "text",
      "text": "You are Meeting Copilot, an in-meeting assistant. Use the supplied context block to answer questions about the meeting subject. Cite specific lines from the context when relevant. Keep answers ≤ 150 words. If the context does not cover the question, say so plainly and label the answer as a general-knowledge guess. Never invent quotes or numbers that are not in the context.",
      "cache_control": {
        "type": "ephemeral"
      }
    },
    {
      "type": "text",
      "text": "# Synthetic Context Block (T-0.5 Spike Filler)\n\nThe following content is repeated synthetic prose used solely to exercise\nAnthropic's prompt-cache pricing tier. It is not real product spec.\n\n## Section 0\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 1\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 2\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 3\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 4\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 5\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 6\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 7\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 8\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 9\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 10\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 11\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 12\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 13\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 14\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 15\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 16\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 17\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 18\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 19\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 20\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 21\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 22\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 23\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 24\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 25\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 26\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 27\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 28\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 29\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 30\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 31\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 32\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 33\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 34\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 35\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 36\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 37\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 38\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 39\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 40\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 41\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 42\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 43\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 44\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 45\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 46\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 47\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 48\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 49\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 50\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 51\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 52\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 53\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 54\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 55\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 56\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 57\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 58\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 59\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 60\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 61\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 62\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 63\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 64\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 65\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 66\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 67\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n## Section 68\n\nThe Meeting Copilot product is a desktop application that captures live meeting audio, transcribes it on-device using MLX whisper, and generates LLM-powered answers to detected questions while leveraging Anthropic's prompt-cache feature so that the same 20-thousand-token context block is not re-billed on every turn. The PRD specifies a first-token latency budget of two seconds, a worst-case real-time-factor of one for the transcription pipeline, and an end-to-end question-to-first-answer-token target of three seconds. Privacy posture: audio never leaves the host machine. Only text — recent transcript snippets and the detected question — flows to Anthropic. Cache breakpoints are placed on the static instructions block and the user-supplied context block, both marked ephemeral with the standard five-minute TTL. The bench harness in T-0.5 of the Phase 0 spike validates that warm-cache calls return their first streamed token in well under two seconds and that cache_read_input_tokens accounts for at least ninety percent of input tokens on every call after the first.\n\n",
      "cache_control": {
        "type": "ephemeral"
      }
    }
  ],
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Recent transcript (last 90s):\nSpeaker 1: Okay so the next thing on the agenda is the Q3 launch timeline. Speaker 2: Right — we said we wanted GA by mid-September. Engineering thinks that's still on. Speaker 1: What about the latency target? Last I heard we were at about 2.4 seconds end-to-end. Speaker 2: We shaved 400 milliseconds off the streaming path last week. Should be at two flat now. Speaker 1: Cool. Also — what's the cost per active user looking like with the prompt cache enabled?"
        },
        {
          "type": "text",
          "text": "Question detected: \"What is the current end-to-end latency and how does prompt cache affect it?\"\n\nĐưa ra answer ngắn gọn (≤ 150 từ), reference context block khi có. Nếu context không cover, nói thẳng \"không có trong context — guess based on general knowledge\"."
        }
      ]
    }
  ]
}