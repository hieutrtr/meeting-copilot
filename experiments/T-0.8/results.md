# T-0.8 — Results (Tauri scaffold)

> Generated by `experiments/T-0.8/run.sh` on 2026-05-05T13:02:53Z.
> Host: macOS 26.2 / Apple M1 Max / 64 GB. Bun 1.3.11. Cargo: missing.
>
> **Headline**: 🟡 **CAVEAT-GO BLOCKED-PENDING-INSTALL** — 7/8 ACs green; AC-7 (`cargo tauri dev`) deferred until Rust toolchain is installed (INDEX §"Known Gaps" #1). Flips to numeric GO with zero code change.

---

## Per-AC verdict

| AC | Bar | Measured | Verdict |
|---|---|---|---|
| AC-1 | Project root layout per ARCH App A: `src-tauri/`, `src/`, `package.json`, `index.html`, `vite.config.ts`, `tsconfig.json`, `Cargo.toml`, `tauri.conf.json`, `build.rs`, `src-tauri/src/{main,lib}.rs`, `capabilities/default.json`, `icons/icon.png` | All 15 path checks (AC-1a..AC-1o) **PASS** | ✅ |
| AC-2 | `package.json` parses + declares `@tauri-apps/api`, `react`, `react-dom`, `@tauri-apps/cli`, `vite`, `@vitejs/plugin-react`, `tauri` script | 8/8 deps + script checks **PASS**. `bun install` succeeded: 71 packages installed in 2.69s. | ✅ |
| AC-3 | `Cargo.toml` declares `tauri@2`, `tauri-build@2`, `serde`, `[lib].crate-type` includes `cdylib` | 4/4 grep gates **PASS** (TOML parser unavailable on host; full validation deferred to `cargo check` post-rust-install) | ✅ structural / 🟡 deferred numeric |
| AC-4 | `tauri.conf.json` valid JSON + `identifier` set + `build.devUrl=http://localhost:1420` + `beforeDevCommand=bun run dev` + `frontendDist=../dist` + window labelled `main` | 6/6 checks **PASS**. `identifier=dev.hieutran.meeting-copilot`, window={label=main, 800×600} | ✅ |
| AC-5 | IPC smoke test wired both sides: `#[tauri::command] fn greet` + `generate_handler![greet]` in `lib.rs`; `invoke<string>("greet", { name })` in `App.tsx`; capability grants `core:default` to window `main` | 7/7 checks **PASS** | ✅ |
| AC-6 | `experiments/T-0.8/run.sh` exits cleanly (0 = full GO, 14 = BLOCKED-PENDING-INSTALL with walkthrough) | exit **14** with full unblock walkthrough printed; structured + reproducible | ✅ (CAVEAT branch) |
| AC-7 | `cargo tauri dev` (or `bun tauri dev`) opens window — capture log marker | **BLOCKED-PENDING-INSTALL** — `cargo`, `rustc` missing on host. Walkthrough printed by `run.sh`. Same pattern as T-0.2 BlackHole / T-0.5 API key. | 🟡 deferred |
| AC-8 | `bun run dev` boots Vite | Vite v5.4.21 ready in **1099 ms**, listening on `http://localhost:1420/` | ✅ |
| AC-2-bonus | `bun run build` surfaces TS errors if any | exit 0; built in 302 ms; `dist/index.html=0.40 kB`, `index.js=143.50 kB / gzip 46.15 kB`, `index.css=0.72 kB`. 34 modules transformed, no warnings. | ✅ |

40/40 structural checks (AC-1..5) + bun install + Vite boot + Vite build all green.

---

## Numeric measurements

| Metric | Value | Source |
|---|---|---|
| Bun install duration | 2.69 s | `bun install` log |
| Bun install package count | 71 | `bun install` log |
| Vite cold-start to "Local:" line | 1099 ms | `vite.log` |
| Vite production build | 302 ms / 34 modules | `bun run build` |
| Frontend bundle (uncompressed) | 143.50 kB JS + 0.72 kB CSS + 0.40 kB HTML | `dist/` |
| Frontend bundle (gzip) | 46.15 kB JS + 0.37 kB CSS + 0.27 kB HTML | `bun run build` |
| Structural checks total | 40 / 40 | `verify.ts` |
| Run script exit code | 14 | `experiments/T-0.8/exit_code` |

---

## Reproduction

```sh
cd meeting-copilot
./experiments/T-0.8/run.sh
```

Logs at `experiments/T-0.8/bench.log` + `vite.log`. After Rust install, the
script auto-detects cargo and runs `cargo tauri dev` for AC-7.

---

## What flips on Rust install

The single cargo-install branch flips:
1. `cargo check -p meeting-copilot` should compile.
2. `cargo test -p meeting-copilot` runs `tests::greet_smoke` + `tests::greet_handles_empty_name` (both pure-fn asserts).
3. `cargo tauri dev` opens an 800×600 window titled "Meeting Copilot" with the IPC smoke UI; clicking the button invokes `greet` and renders `Hello Meeting Copilot, from Tauri!`.

No code changes required. The `run.sh` already detects cargo and switches to the live branch.

---

## CAVEAT-GO rationale (for spike memo / PHASE-0-COMPLETE)

The Tauri scaffold is structurally sound:
- Every file ARCH §App A calls for at the project root exists, parses, and contains the right tokens.
- IPC plumbing is symmetric across the language boundary (Rust `greet` ↔ TS `invoke`).
- Frontend toolchain works end-to-end: `bun install` + `bun run dev` + `bun run build` all exit 0.
- The unblocking step (AC-7) is fully described in INDEX §"Known Gaps" #1 — exactly one shell session, no code change.

This mirrors the BLOCKED-PENDING-INSTALL pattern from T-0.2 (BlackHole) and BLOCKED-PENDING-KEY from T-0.5 (Anthropic API key): the spike has proven everything that doesn't require external installs; the residual numeric verification flips automatically post-install.
