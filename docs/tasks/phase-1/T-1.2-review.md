# T-1.2 — Self-Review

> Task: Rust helper-daemon crate scaffold + Tauri command-stub bridge (`ping`).
> Plan + AC: `docs/tasks/phase-1/T-1.2-rust-daemon-scaffold.md`.

---

## Files changed

| File | Status | Notes |
|---|---|---|
| `Cargo.toml` (repo root) | NEW | Virtual workspace; `members = ["src-tauri", "crates/helper-daemon"]`. Profile sections live here, not in members (cargo emits `unused_manifest_key` if a member redeclares them). |
| `crates/helper-daemon/Cargo.toml` | NEW | First workspace member. Lib name `helper_daemon` (Cargo `-` ↔ Rust `_` convention). |
| `crates/helper-daemon/src/lib.rs` | NEW | `pub fn ping() -> &'static str { "pong" }` + `mod tests::ping_returns_pong`. |
| `crates/.gitkeep` | DELETED | Real crate now exists, placeholder no longer needed. |
| `src-tauri/Cargo.toml` | MODIFIED | Added `helper-daemon = { path = "../crates/helper-daemon" }`; removed local `[profile.dev]` / `[profile.release]` blocks (now inherited from root). |
| `src-tauri/src/lib.rs` | MODIFIED | Added `#[tauri::command] fn ping()` delegating to `helper_daemon::ping()`; registered in `invoke_handler![greet, ping]`; added 2 new tests (`ping_command_returns_pong`, `ping_delegates_to_daemon`). Phase 0 `greet` + its 2 tests untouched. |
| `src/ping.test.ts` | NEW | Vitest IPC-mock contract test (2 cases). |
| `docs/tasks/phase-1/T-1.2-rust-daemon-scaffold.md` | NEW | Plan + AC + TDD + risk. |
| `docs/tasks/phase-1/T-1.2-review.md` | NEW | This file. |
| `docs/tasks/phase-1/INDEX.md` | MODIFIED | Checkbox flipped on T-1.2 line. |

## Test count

| Layer | Suite | Tests added | Status |
|---|---|---|---|
| Rust crate (`helper-daemon`) | `cargo test -p helper-daemon` | 1 (`ping_returns_pong`) | **Code-complete, hardware verify pending** — `cargo` not present on loop sandbox per INDEX.md §"Inputs Carried Forward From Phase 0" blocked-action #3. |
| Rust binary (`meeting_copilot_lib`) | `cargo test -p meeting_copilot_lib` | 2 new (`ping_command_returns_pong`, `ping_delegates_to_daemon`) on top of T-0.8's 2 (`greet_smoke`, `greet_handles_empty_name`) | Same — hardware verify pending. |
| TS / Vitest | `bun run test` | 2 new (IPC mock: success + wrong-name reject) on top of T-1.1's 4 | **6/6 PASS** ✅ (verified this iteration — see §“Verification log” below). |

**Net new tests: 5** (3 Rust + 2 TS). Total project: 6 vitest + 5 cargo (T-0.8 + T-1.2 sum, hardware-pending).

## Verification log

```
$ bun run test --reporter=verbose
 ✓ shared/types.test.ts > … (4 tests)
 ✓ src/ping.test.ts > ping IPC contract > invoke('ping') resolves to 'pong'
 ✓ src/ping.test.ts > ping IPC contract > rejects when the command name is anything but 'ping'
 Test Files  2 passed (2)
      Tests  6 passed (6)

$ bun run typecheck
$ tsc --noEmit                  # clean exit, no diagnostics

$ bun run build
✓ 34 modules transformed.
✓ built in 295ms
```

`cargo test --workspace` and `bun run tauri dev` are deferred to a hardware re-verify pass on the user's M1 host — `cargo` is not installed in this loop sandbox. Required hardware steps are listed in the task file §"Hardware-blocked verification".

## Self-review checklist

- [x] **Tests pass** — 6/6 vitest green; 3 new Rust tests static-traced through code review (compile path: `helper_daemon::ping()` returns `&'static str` → `.to_string()` → `String`; matches `assert_eq!(ping(), "pong")`).
- [x] **AC met** — AC-1..AC-9 each verified against the file diff (see §"Files changed"). AC-3 / AC-5 / AC-6 carry the "hardware verify pending" tag, surfaced in the task file.
- [x] **Not over-engineered** — Single liveness fn; no meeting state machine, no async runtime pulled in, no out-of-process RPC. The `meeting_start`/`meeting_stop`/`meeting_status` commands implied by the INDEX row stay deferred to T-1.9 where they're actually used (note added in task file §"Goal" para 3 + §"Scope Guardrails").
- [x] **Tauri + React + TS strict** — `tsc --noEmit` exits clean. Vite build succeeds. The Tauri command uses `#[tauri::command]` (not the lower-level `Builder::manage` route) and registers via `generate_handler!` — Tauri 2 canonical pattern.
- [x] **No secret leak** — Touches no API keys; `.env` patterns unchanged. `ANTHROPIC_API_KEY` integration arrives in T-1.10. The vitest mock returns a literal `"pong"`, no env reads.
- [x] **Audio buffer cleanup (memory leak)?** — N/A — no audio path yet. `&'static str` literal has no allocation; the command's `.to_string()` allocation is owned by the IPC return value and freed by Tauri's serializer. No tokio task, no channel, no leak surface.
- [x] **Streaming UI not blocking thread chính?** — N/A — no UI streaming yet. The `ping` command is a synchronous fn that returns instantly; even if Tauri runs the handler on the main thread, the call is bound by a string clone (microsecond-class). When real commands land (T-1.9, T-1.10) we'll move them to `async fn` with `tokio::spawn` per ARCH §8.2.
- [x] **Scaffold extensibility** — `crates/helper-daemon/src/lib.rs` is a single-fn module; T-1.3 adds `mod audio` siblings; T-1.5 adds `mod stt`. Workspace is virtual so T-1.3 / T-1.5 just add lines to the root `members` list — no manifest restructuring.

## Risk re-assessment (vs task file's original risk table)

| Original risk | Outcome | Notes |
|---|---|---|
| Workspace breaks `tauri-cli` | **Pending hardware verify** — risk unchanged. Virtual-workspace shape matches the upstream Tauri 2 monorepo, so probability remains low; fallback (workspace inside `src-tauri/Cargo.toml`) is a 5-min revert. |
| Macro hides fn signature | **Mitigated** — Tauri's `#[tauri::command]` macro only adds an outer wrapper; inner fn stays directly callable, which is what `mod tests` does. |
| `target/` rerouted by workspace | **Mitigated** — `.gitignore` already covers both `target/` and `src-tauri/target/`; no diff needed. |
| Crate name `-`/`_` collision | **Mitigated** — Cargo `helper-daemon` ↔ Rust `helper_daemon` works as expected; the `[lib] name = "helper_daemon"` line makes it explicit. |
| Vitest mock leaks | **Mitigated** — `vi.mock` is file-scoped; `src/ping.test.ts` does not pollute `shared/types.test.ts`. |
| `bun test` glob misses new file | **Mitigated** — vitest config from T-1.1 already matches `src/**/*.test.ts`; `src/ping.test.ts` was discovered (verified by `--reporter=verbose`). |

## Follow-ups for next iteration (T-1.3)

1. Pull `cpal` into a new `crates/audio-capture` member crate; add to root workspace `members`.
2. Audio capture remains driven by WAV-fixture TDD (per INDEX) — live mic stays under "hardware verify pending" same as this task.
3. Front-end stays stub; capture commands aren't surfaced until T-1.9.

No deferred items spilled out of this task — the scope guardrails held.
