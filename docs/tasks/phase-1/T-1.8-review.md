# T-1.8 — ContextLoader (single file) — Self-Review

> Loop position 9/16. Task spec: `T-1.8-context-loader.md`.

## Files changed

| Path | Kind | Net lines |
|---|---|---|
| `docs/tasks/phase-1/T-1.8-context-loader.md` | docs (new) | +88 |
| `docs/tasks/phase-1/T-1.8-review.md` | docs (this file) | new |
| `crates/helper-daemon/src/context.rs` | rust src + tests (new) | +180 |
| `crates/helper-daemon/src/lib.rs` | rust src (re-exports + module mount) | +4 |
| `src-tauri/src/lib.rs` | rust src + tests (Tauri command + 2 tests) | +30 |
| `src/store/contextStore.ts` | TS src (new) | +56 |
| `src/store/contextStore.test.ts` | TS tests (new) | +96 |
| `src/components/ContextLoader.tsx` | React src (new) | +66 |
| `src/components/ContextLoader.test.tsx` | React tests (new) | +101 |
| `package.json` | dep entry (`zustand: ^5.0.13`) | +1 |
| `bun.lock` | lockfile | regen |

## Test count

**Vitest (TS / React):**
- Store: **5** new — `CTX-S1`, `CTX-S2` (Error obj + string variant), `CTX-S3`, plus the prior-error-cleared regression case.
- Component: **5** new — `CTX-U1`, `CTX-U2`, `CTX-U3`, the empty-path-disabled case, and the Clear-button visibility case.
- Total new: **10**. Suite total: **25** (15 prior + 10).

`bun run test` → `Test Files 6 passed (6); Tests 25 passed (25)` (749 ms).
`bun run typecheck` → clean.

**Cargo (Rust):**
- `crates/helper-daemon/src/context.rs`: 8 new — `estimate_tokens_rounds_up`, `reads_md_file`, `reads_txt_file`, `rejects_pdf_extension`, `rejects_extensionless_path`, `rejects_missing_file`, `serde_json_uses_camel_case`, `accepts_uppercase_extension`.
- `src-tauri/src/lib.rs`: 2 new — `read_context_file_command_rejects_pdf`, `read_context_file_command_round_trips_md`.
- Total new: **10**. Suite cumulative: **52 + 10 = 62** (rust toolchain still missing on this loop sandbox per blocked-action #3 — code-complete, hardware verify pending).

## AC checklist

- [x] **AC-1** `read_context_file` returns `ContextFile { path, content, char_count, estimated_tokens }` — covered by `reads_md_file` / `reads_txt_file`.
- [x] **AC-2** `.md` / `.txt` only; PDF + extensionless rejected — `rejects_pdf_extension`, `rejects_extensionless_path`. Bonus uppercase test covers `.MD`.
- [x] **AC-3** Typed `ContextError::Io` for missing file — `rejects_missing_file`.
- [x] **AC-4** `estimated_tokens = ceil(char_count / 4)` — `estimate_tokens_rounds_up` covers 0 / 1 / 4 / 5 / 8 / 9 / 100 / 101.
- [x] **AC-5** Tauri command `read_context_file` registered + delegates to helper-daemon — `invoke_handler!` updated, `read_context_file_command_*` smokes the wrapper. Camel-case JSON shape verified by `serde_json_uses_camel_case`.
- [x] **AC-6** zustand store `useContextStore` with `source / content / error / loadFromPath / clear` — all 4 store tests green.
- [x] **AC-7** `ContextLoader` component — input, button, readout, alert; CTX-U1/U2/U3 + 2 affordance tests green.
- [x] **AC-8** No new prod dep beyond zustand (3 KB gzipped). No `tiktoken-js`. No `react-window`.

## Self-review

- [x] Tests pass (10 new vitest; 10 new cargo — code-complete, runtime gated on rust toolchain).
- [x] AC met (8/8 from task doc).
- [x] No over-engineering. Single-file MVP per loop INDEX. The richer file-picker dialog (`tauri-plugin-dialog`) and drag-and-drop are explicitly deferred to T-1.9 / Phase 1.x respectively, with the rationale captured in the task doc §"Out-of-scope".
- [x] Tauri + React + TS strict — `tsc --noEmit` clean. `serde(rename_all = "camelCase")` keeps the IPC payload byte-identical with `shared/types.ts:ContextSource`.
- [x] **No secret leak (`ANTHROPIC_API_KEY`).** This task does not touch the Claude API surface. T-1.10 (Claude streaming Q&A) is the gate where the env-var lands.
- [x] **Audio buffer cleanup.** N/A — this task is text-only file IO. The audio path landed in T-1.3/T-1.4 with explicit drop-no-panic + Arc-tracer leak proofs already.
- [x] **Streaming UI non-blocking.** The `loadFromPath` action is `async` and zustand `set()` is synchronous + side-effect-free. No render blocks — confirmed by the busy-state pattern (`Loading…` flips to `Load file` after the Promise resolves) and no event-loop blocking inside the store.
- [x] No `unwrap()` on the IPC path. The Rust command stringifies `ContextError`; the React store catches and stores `error`. No `panic!` reachable from a malformed input path.

## Risks revisited

- **`chars/4` token-estimate accuracy** — flagged for T-1.10 review. Plain ASCII PRDs land within ~5 % of the real Claude tokenizer. Vietnamese / mixed-script content can drift up to 30 %; if T-1.10's cost AC ($0.015/warm-call advisory) misses by > 1.5×, swap to `tiktoken-rs` in helper-daemon. Phase 1.x escape hatch documented.
- **File-picker UX deferred** — `PHASE-BROWSER-TEST.md` step 2 must explicitly say "paste full path" until T-1.9 adds the dialog plugin. Captured in the task doc.
- **Path information disclosure** — Phase 1 demo runs locally with no network surface. Phase 3 (multi-tenant cloud mode) adds path allow-listing per ARCH §11.

## Out-of-scope captured

- Multi-file aggregation, drag-and-drop, URL/repo-ref/pasted-text sources, real `tiktoken`, vector retrieval, dialog plugin → all listed in `T-1.8-context-loader.md` §"Out-of-scope".

## Loop progression

T-1.8 unblocks T-1.9 (Start Meeting controls — uses `useContextStore` to gate the Start button on a loaded source) and T-1.10 (Claude client — reads `useContextStore.getState().content` to build the cached system block per ARCH §6.2).
