# T-0.6 — Spike Memo (Phase 0 synthesis)

> Source: `docs/IMPLEMENTATION-PLAN.md` line 32 (raw plan T-0.6 = "Viết Spike Memo (1-2 trang): kết quả 3 prove, recommend stack").
> Loop position: step 7/11. Dep: T-0.1 .. T-0.5 reviews complete.
> Architecture references: §1 (overview), §2.1 (BlackHole default), §2.2 (alternatives), §3.2 (MLX vs Deepgram), §6.2 + §7.2 (cache), §8.1 (Tauri vs Electron), §11 (privacy modes), §12 (latency budget), §13 (cost).
> PRD reference: §1 (problem), §2 (target user / Apple Silicon constraint).

## 1. Acceptance criteria (from plan)

| AC | Spec | How to verify |
|---|---|---|
| AC-1 | Memo committed to `docs/spike-memo.md` | File exists, ≤ 2 pages (~800–1200 words target per loop framework) |
| AC-2 | Go/no-go for each of the 3 unknowns | Each unknown gets an explicit verdict line with measured numbers |
| AC-3 | Recommended stack named | Tauri vs Electron called; MLX vs Deepgram default called |
| AC-4 | Risks surfaced in spike captured | At least 3 risks not in original PRD/ARCH risk register |
| AC-5 | Pivot suggestions if any spike is CAVEAT | T-0.3 (mic+sys) is CAVEAT-GO and T-0.5 (cache) is CAVEAT-GO; both need pivot ladders cited |

This is a **doc task** per INDEX.md §"Spike vs TDD" — no benchmark or code. Verification is by file existence + content-presence check, not numeric AC.

## 2. Approach

Pure synthesis. Inputs (read-only):

| Input | What I extract |
|---|---|
| `docs/tasks/phase-0/T-0.1-review.md` Findings | mic capture verdict + TCC pre-flight pattern |
| `docs/tasks/phase-0/T-0.2-review.md` Findings | system-audio (BlackHole) verdict + device redirect pattern |
| `docs/tasks/phase-0/T-0.3-review.md` Findings + Pivot ladder | simultaneous-capture verdict + Pivot 1/2/3 |
| `docs/tasks/phase-0/T-0.4-review.md` Findings | RTF numbers + recommended Phase 1 default model |
| `docs/tasks/phase-0/T-0.5-review.md` Findings | request-shape verdict + cache-ratio plumbing + BLOCKED-on-key state |
| `experiments/T-0.4/results.{md,json}` | canonical RTF table + per-model headline |
| `docs/IMPLEMENTATION-PLAN.md` line 38 | Phase exit criterion ("if MLX not fast enough → Deepgram default Phase 1") |
| `docs/ARCHITECTURE.md` §8.1 | Tauri vs Electron decision rationale |

No new measurements; this task only renders evidence already collected.

## 3. Memo structure (one page, ~1000 words)

1. **TL;DR** (3 lines) — 3 verdicts in a table.
2. **Unknown #1 — Audio capture** (~250 words) — split mic / system / simultaneous; cite pivots.
3. **Unknown #2 — MLX whisper RTF** (~200 words) — headline numbers + recommended default + VAD caveat.
4. **Unknown #3 — Claude streaming + cache** (~200 words) — structural verdict + BLOCKED-on-key disclaimer.
5. **Stack decision** (~150 words) — Tauri stays, MLX default with Deepgram fallback ready.
6. **Risks surfaced in spike** (~100 words, bullets) — drift unmeasured, runaway-decode hallucination, cache TTL, host vs target spec.
7. **Pivot ladder ready for Phase 1** (~80 words, bullets) — Aggregate Device → ScreenCaptureKit → Electron+helper; MLX → Deepgram default.
8. **Phase 1 readiness** (1 line) — green to proceed pending two user actions (BlackHole install, API key export).

## 4. Notes / open questions

- The host is M1 Max / 64 GB; the target is M1 Pro / 16 GB. T-0.4 already documents the 1.7× scaling factor used for conservative projection. Memo must repeat this caveat so a reader doesn't over-quote the M1 Max numbers.
- Two CAVEAT-GO verdicts (T-0.3 unknown #1, T-0.5 unknown #3) — neither blocks Phase 1 *kickoff* (the unblock is user-side install/key, not a code pivot), so the overall Phase 0 verdict can still be GREEN if both gating CAVEATs are about user environment rather than measured failure. Memo must not over-claim — explicitly call them BLOCKED-on-environment, not GO.
- T-0.5 advisory cost AC was revised mid-task ($0.005 → $0.015). Memo should restate the as-shipped advisory bar, not the original.
- T-0.7 (next step) is the stakeholder review of *this* memo. So this memo's structure must be reviewable at a glance — bullets > paragraphs where possible.

## 5. Out of scope for T-0.6

- New code or experiments
- Re-running any T-0.1..T-0.5 measurements
- Editing PRD or ARCHITECTURE (read-only spec per loop constraints)
- Repo skeleton (T-0.8)
- Phase 1 task breakdown (Phase 1 territory)

## 6. Reproduction

Memo is plain markdown. Re-render = `cat docs/spike-memo.md`. No build step.

## 7. Done check (mirrors loop rule 4 spike memo bullets)

- [ ] `docs/spike-memo.md` exists
- [ ] 3 unknowns each get GO / CAVEAT / NO-GO + measured numbers (where available) + BLOCKED disclosure (where applicable)
- [ ] Recommended stack stated (Tauri kept; MLX default with Deepgram cited as fallback)
- [ ] Risks surfaced in spike enumerated
- [ ] Pivot suggestions for Phase 1 enumerated
- [ ] Within ~800–1200 words target
- [ ] `docs/tasks/phase-0/T-0.6-review.md` exists with self-review checklist
- [ ] `docs/tasks/phase-0/INDEX.md` checkbox flipped with one-line headline
