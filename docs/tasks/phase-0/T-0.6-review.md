# T-0.6 — Spike Memo — Self-review

> Reviews `docs/tasks/phase-0/T-0.6-spike-memo.md` and the deliverable `docs/spike-memo.md`. Outcome: **🟢 GO** — memo synthesises the 3 verdicts with measured numbers (where measurable) and explicit BLOCKED-on-environment disclosure (where applicable), recommends stack, lists 5 spike-discovered risks and 6-step pivot ladder for Phase 1.

## Files / artefacts changed

- `docs/tasks/phase-0/T-0.6-spike-memo.md` (new) — task plan + AC + ARCH refs + memo structure
- `docs/spike-memo.md` (new) — Phase 0 deliverable (~1100 words, 1-2 pages)
- `docs/tasks/phase-0/INDEX.md` — T-0.6 checkbox flipped, headline appended (sibling edit)
- `docs/tasks/phase-0/T-0.6-review.md` (this file)

No experimental code, no `experiments/T-0.6/` — this is a pure synthesis task per INDEX §"Spike vs TDD".

## Self-review checklist

- [x] **AC measured numerically?** Doc task — AC is content-presence, not numeric. Done check from the task spec is fully covered: 3 unknowns each get GO / CAVEAT verdict + measured numbers (RTF, char counts, exit codes) + BLOCKED disclosure for the two unknown user-environment blockers; recommended stack stated (Tauri kept, MLX `medium` default with Deepgram fallback); 5 spike-surfaced risks listed; 6-row pivot ladder; Phase 1 readiness gate stated. Word count 1100, within "1-2 page" AC; 30% over the loop framework's ~800–1200 advisory because the table-heavy format inflates `wc -w` and the content is non-redundant.
- [x] **Reproducible (1 command)?** Yes — `cat docs/spike-memo.md`. Markdown, no build step. The numbers cited are link-back-to-source (T-0.{1..5}-review.md + experiments/T-0.{1..5}/results.{md,json}) so any reader can spot-check by re-reading the source review.
- [x] **Failure mode documented?** N/A for a doc task. The memo *itself* documents two BLOCKED states (T-0.3 BLOCKED-PENDING-INSTALL, T-0.5 BLOCKED-PENDING-KEY) and the unblock paths for each — that's the closest equivalent.
- [x] **Throwaway code in `experiments/`?** N/A — no code in T-0.6. `docs/spike-memo.md` lives at the documented memo path per INDEX §"Outputs Per Task" → "Final phase outputs". Not throwaway; this *is* the deliverable.

## Findings (memo of the memo)

1. **Two CAVEAT-GOs converge on the same shape: "code path proven, hardware/account blocker outside our control".** T-0.3 BLOCKED-PENDING-INSTALL on BlackHole-2ch + AMS setup; T-0.5 BLOCKED-PENDING-KEY on `ANTHROPIC_API_KEY` env var. Both flip to numeric GO/NO-GO with **zero code changes** post-unblock. This is the right shape for a spike — code is ready, environment is what's missing — and it lets Phase 0 sign off green even though two of three unknowns are not yet numerically gated. The memo is explicit about this distinction.
2. **The unanimous spike recommendation tightens ARCH §2.4 from "VAD nice-to-have" to "VAD required".** The runaway-decode failure mode on `large-v3-turbo` (RTF 0.041 → 0.221–0.256 on padded silence with hallucinated `Good Good Good...` output) is the single most actionable finding from Phase 0. Memo §"Risks surfaced" #1 carries it forward to Phase 1.
3. **Stack decisions are stable; no pivots fire preemptively.** Tauri stays (ARCH §8.1 reinforced by spike — Swift→Rust mapping is one `AudioUnitSetProperty` call). MLX local stays default (`medium` for Phase 1, `large-v3-turbo` post-VAD). Deepgram Nova-2 stays *named* fallback, not default. All three are conditional on the post-install + post-key re-measurement landing inside the projected windows.
4. **Phase 1 has 6 named pivot triggers ready to fire** — drift > 100 ms (Aggregate Device), both modes fail drift (ScreenCaptureKit), ScreenCaptureKit also fails (Electron + helper), M1 Pro RTF > 0.7 (drop tier), cold-start blocker (warm pool), cache ratio < 0.9 (1-hour TTL retry). Each has a trigger criterion + cost estimate. This means Phase 1 task breakdown can be authored against measured numbers, not against speculation.
5. **The host vs target spec gap (M1 Max / 64 GB vs M1 Pro / 16 GB) is documented in 3 places** — INDEX, T-0.4 review, spike memo §"Caveats" — and the conservative 1.7× scaling factor reaches every projected number. No risk of over-quoting M1 Max numbers downstream.

## Issues / pivot suggestions

- **Memo is 30% over the framework's ~800–1200 word advisory.** Trimming would lose measured numbers, which are the core deliverable of a spike memo. Decision: keep length, document the deviation here. Plan AC is "1-2 page" which is satisfied.
- **No live numbers from T-0.5 yet.** AC-3 / AC-4 / AC-6 from T-0.5 will be filled in by the user post-key-export. The memo's TL;DR row for unknown #3 is intentionally non-numeric on the latency axis; flipping it requires re-running the bench and re-running this memo (or T-0.7 reviewer can amend in-place). Documented contract.
- **No M1 Pro re-measurement.** Per INDEX, this is a Phase 1 pre-ship gate (T-1.12 E2E test thủ công), not a Phase 0 deliverable. Memo cites it as a Phase 1 readiness item, not a Phase 0 blocker.
- **T-0.7 reviewer (next loop step) gets a structured target.** The memo deliberately uses tables for the TL;DR, the pivot ladder, and the unknowns, so a reviewer can sanity-check the verdicts without re-reading prose. Risks + pivots are bullet lists for the same reason.

## Verification (loop step 7/11 done-criteria)

- [x] `docs/tasks/phase-0/T-0.6-spike-memo.md` exists with AC + ARCH refs + reproduce
- [x] `docs/tasks/phase-0/T-0.6-review.md` exists (this file) with self-review + 5 findings + issues
- [x] `docs/spike-memo.md` exists with: TL;DR verdict table, 3 per-unknown sections (each with measured numbers and/or BLOCKED disclosure), stack decision (Tauri / MLX `medium` / Deepgram fallback), 5 spike-surfaced risks, 6-row pivot ladder, Phase 1 readiness gate
- [x] Word count ~1100 (1-2 pages); 30% over loop advisory but within plan AC ("1-2 trang")
- [x] No new measurements; no edits to PRD or ARCHITECTURE (read-only spec respected)
- [x] No experimental code created (doc task per INDEX §"Spike vs TDD")
- [x] INDEX.md checkbox flipped to `[x]` with one-line headline (sibling of this file)
- [x] Numeric Go-call recorded: 🟢 GO — Phase 0 spike memo authored; ready for T-0.7 (stakeholder review).
