# T-0.7 — Stakeholder Review — Self-review

> Reviews `docs/tasks/phase-0/T-0.7-stakeholder-review.md`. Outcome: **🟢 GO** — stakeholder verdict (APPROVED-WITH-CONDITIONS) is recorded with 3 actionable conditions (C-1, C-2, C-3); none blocks T-0.8 (Tauri scaffold) which is the next loop step.

## Files / artefacts changed

- `docs/tasks/phase-0/T-0.7-stakeholder-review.md` (new) — task plan + AC + the actual stakeholder verdict (combined; deliverable is small)
- `docs/tasks/phase-0/T-0.7-review.md` (this file) — self-review of the T-0.7 task
- `docs/tasks/phase-0/INDEX.md` — T-0.7 checkbox flipped, headline appended (sibling edit)

No experimental code, no `experiments/T-0.7/` — pure doc task per INDEX §"Spike vs TDD". `docs/spike-memo.md` is **not** edited; the memo is the artefact being reviewed and the conditions C-1/C-2/C-3 are deferred to PHASE-0-COMPLETE / Phase 1 kickoff per §4 of the review file.

## Self-review checklist

- [x] **AC measured numerically?** Doc task — AC is content-presence, not numeric. Plan AC = "Memo approved hoặc list pivot cần làm" → satisfied: explicit verdict line at the top of the review file (🟢 APPROVED-WITH-CONDITIONS), 3 concrete conditions listed with memo line citations + actionable steps. Spot-check table in §5 proves the underlying memo numbers were cross-referenced.
- [x] **Reproducible (1 command)?** Yes — `cat docs/tasks/phase-0/T-0.7-stakeholder-review.md`. Markdown, no build step. Spot-check trail in §5 makes every accept-as-is claim re-verifiable by `cat`-ing the cited source.
- [x] **Failure mode documented?** N/A for a doc task. The review *itself* documents three soft-failure modes of the memo (C-1 phase confusion + scope drift, C-2 unsourced 1.7× factor, C-3 missing cost math) and assigns each a severity + window. None blocks the next step.
- [x] **Throwaway code in `experiments/`?** N/A — no code in T-0.7. Review file lives at the per-INDEX path under `docs/tasks/phase-0/`. Not throwaway; it *is* the deliverable.

## Findings (carry-forward to PHASE-0-COMPLETE.md)

1. **Memo passes stakeholder review without rewrite.** All 3 unknowns' verdicts and headline numbers cross-checked against `experiments/T-0.{1..5}/results.{md,json}` and the per-task review files; no number is fabricated or misquoted. Both CAVEAT-GO disclosures are honest about measured vs environment-blocked. Stack decision (Tauri / MLX `medium` / Deepgram fallback) is supported by spike evidence. The 5 spike-surfaced risks are genuinely novel relative to the PLAN's top-5 risk register.
2. **Three surgical conditions, not a rewrite.** C-1 (T-1.5 VAD scope drift — and a "Phase 2 / T-1.5" phase confusion since T-1.5 is in Phase 1) is the most consequential and gets a concrete recommendation (insert a `T-1.5b` for VAD + `large-v3-turbo` upgrade gate). C-2 (1.7× scaling factor lacks an explicit "this is a rule-of-thumb upper bound" footnote) and C-3 (cost AC revision lacks the math breakdown $0.009 output + $0.0065 cached input ≈ $0.015) are low-severity transparency fixes.
3. **None of the conditions blocks T-0.8 (Tauri scaffold).** The scaffold is environment-independent of BlackHole / API key / VAD scope. Loop can advance to T-0.8 without addressing C-1/C-2/C-3. The conditions become inputs to PHASE-0-COMPLETE.md (loop step 11/11) and Phase 1 kickoff.
4. **Pivot ladder is armed but quiescent.** All 6 pivots are gated on numbers that don't yet exist (post-install drift, M1 Pro re-measurement, post-key cache ratio). This is the correct shape — pivots fire on measured numbers, not speculation.
5. **Solo-dev "self as stakeholder" is honest about its limits.** Review §10 explicitly notes that a second-pair-of-eyes pass would add value if a contributor joins; this is not pretending to be a full external review.

## Issues / pivot suggestions

- **Inline plan + deliverable in one file.** I kept T-0.7's task plan AND the stakeholder verdict in a single `T-0.7-stakeholder-review.md` (rather than splitting plan + deliverable like T-0.6 did with `T-0.6-spike-memo.md` + `docs/spike-memo.md`). Justification: T-0.7's deliverable is a 1-page review doc that lives under `docs/tasks/phase-0/`, not a top-level `docs/` artefact. Splitting would have produced two files where one is enough. Documented in the review file's intro for the audit trail.
- **No `experiments/T-0.7/` directory.** Per INDEX §"Spike vs TDD", T-0.7 is a doc task — no experimental code expected. Loop framework's "throwaway code in `experiments/`" check is N/A here.
- **C-1 should land in PHASE-0-COMPLETE.md "Lessons learned" section.** Otherwise risk that T-1.5 starts in Phase 1 with raw-plan scope and the VAD requirement is silently lost. Tracked as a follow-up for loop step 11/11.

## Verification (loop step 8/11 done-criteria)

- [x] `docs/tasks/phase-0/T-0.7-stakeholder-review.md` exists with stakeholder verdict line + 3 concrete conditions + spot-check table
- [x] `docs/tasks/phase-0/T-0.7-review.md` exists (this file) with self-review checklist + 5 findings + issues
- [x] No edits to read-only spec (`docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION-PLAN.md`) and no edits to `docs/spike-memo.md` (the artefact being reviewed)
- [x] No experimental code created (doc task per INDEX §"Spike vs TDD")
- [x] INDEX.md checkbox flipped to `[x]` with one-line headline (sibling edit)
- [x] T-0.8 (next loop step) is unblocked — none of C-1/C-2/C-3 gates it
- [x] Verdict recorded: 🟢 APPROVED-WITH-CONDITIONS — proceed to T-0.8 Tauri scaffold; carry C-1/C-2/C-3 into PHASE-0-COMPLETE.md
