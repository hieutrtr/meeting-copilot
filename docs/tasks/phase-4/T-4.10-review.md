# T-4.10 Review — MCP dispatch E2E (Telegram → bridge → meeting-copilot)

> Self-review per `docs/tasks/phase-4/INDEX.md` §"Process Rules" Rule 3.
> Spec: `docs/tasks/phase-4/T-4.10-mcp-dispatch-e2e.md`.

## 1. Files Touched

| Path | Action | LOC delta (approx) |
|---|---|---|
| `src/mcp/e2eHarness.ts` | new | +210 |
| `src/mcp/e2eHarness.test.ts` | new | +320 (21 tests) |
| `src/mcp/bin.ts` | edit | +25 / −2 (env-gated branch) |
| `tests/e2e/__helpers__/spawn-mcp-server.ts` | new | +180 |
| `tests/e2e/__fixtures__/seed-meeting.json` | new | +35 (data) |
| `tests/e2e/mcp-dispatch.e2e.test.ts` | new | +320 (5 scenarios) |
| `vitest.config.ts` | edit | +6 / −1 (e2e include + 30s timeout) |
| `docs/tasks/phase-4/T-4.10-mcp-dispatch-e2e.md` | new | task spec |
| `docs/tasks/phase-4/T-4.10-review.md` | new | this file |
| `docs/tasks/phase-4/INDEX.md` | edit | +1 / −1 (checkbox flip) |

Total ≈ 1,100 LOC of code + tests + docs (within INDEX budget of 400–550 LOC code; tests + docs push the gross delta higher per the same convention as T-4.5 / T-4.7 / T-4.9).

## 2. Test Run Summary

- **Full TS suite**: `bun run test` — **57 files / 900 tests / 0 fail / 0 skip** (pre-T-4.10 baseline was 55 / 874 from T-4.9; +21 in `src/mcp/e2eHarness.test.ts` + +5 in `tests/e2e/mcp-dispatch.e2e.test.ts` = +26 net).
- **Typecheck**: `bun run typecheck` — clean (`tsc --noEmit` exits 0).
- **E2E determinism (AC-1)**: 3/3 consecutive runs of `bun run test tests/e2e/mcp-dispatch.e2e.test.ts` — all 3 exits 0, 5/5 scenarios green each run, durations 553 / 559 / 567 ms.
- **Cargo suite**: T-4.10 is TS-only — no Rust files touched. Phase 0 carry-forward sandbox cargo gate unchanged from T-4.9.

### `e2eHarness.test.ts` — what each test covers

| # | Test | Asserts |
|---|---|---|
| 1 | `loadE2EFixture > parses a minimal valid fixture` | tmpdir + exportRoot round-trip |
| 2 | `loadE2EFixture > preserves optional fields on round-trip` | handshake / appBundlePath / spawnLogPath survive serialize → load |
| 3 | `loadE2EFixture > throws on a missing file` | E2EFixtureInvalid (cause=ENOENT) |
| 4 | `loadE2EFixture > throws on non-JSON content` | E2EFixtureInvalid (cause=SyntaxError) |
| 5 | `loadE2EFixture > throws on a JSON array` | top-level must be object |
| 6 | `loadE2EFixture > throws on missing tmpdir` | required-field error mentions `'tmpdir'` |
| 7 | `loadE2EFixture > throws on missing exportRoot` | required-field error mentions `'exportRoot'` |
| 8 | `buildE2EHandlers > returns a handler for every TOOL_NAMES entry` | all 5 tools wired |
| 9 | `buildE2EHandlers > no extra keys beyond TOOL_NAMES` | sorted keys equality |
| 10 | `install handler > resolves bundle absence through fixture.appBundlePath` | text envelope `"installed": false` |
| 11 | `start handler > appends a JSON line to spawnLogPath on a valid start` | spawn log shows 1 line, cmd=`open`, args[0] starts with `meeting-copilot://start?` |
| 12 | `start handler > returns the fixture handshake on the structured envelope` | meetingId / pid round-trip |
| 13 | `start handler > PrivacyModeViolation gate fires even with fixture-backed deps` | isError + envelope text + spawn log absent |
| 14 | `start handler > ContextNotFound gate fires for traversal in contextPaths` | isError + envelope text + spawn log absent |
| 15 | `status handler > returns fixture.statusReply.meetings` | structured envelope meetings array |
| 16 | `status handler > empty meetings when fixture has no statusReply` | default-empty path |
| 17 | `stop handler > returns fixture.stopReply on the structured envelope` | durationSec / questionCount round-trip |
| 18 | `stop handler > MeetingNotFound when fixture has no stopReply` | isError + envelope text |
| 19 | `export handler > reads from fixture.snapshot and writes to fixture.exportRoot` | path on disk + body contains seeded text + `> Privacy mode:` line |
| 20 | `export handler > MeetingNotFound when snapshot is absent` | isError envelope |
| 21 | `export handler > MeetingNotFound when meetingId mismatches snapshot` | id-mismatch path |

### `tests/e2e/mcp-dispatch.e2e.test.ts` — what each scenario covers

| # | Scenario | Asserts (per AC-2..AC-6) |
|---|---|---|
| 1 | **A. install-check** | `installed:false`, `downloadUrl` matches `/github\.com\|releases/`, `expectedVersion` matches `^\d+\.\d+\.\d+`, spawn log absent. |
| 2 | **B. start-and-export** | Start envelope `meetingId === seed.meeting.id` + `uiUrl` matches embed prefix; spawn log records exactly 1 entry with `cmd === "open"` and `args[0]` matches `^meeting-copilot:\/\/start\?`; export envelope path matches `<workDir>/exports/m_e2e.md`; on-disk markdown contains both seeded chunk texts and the meeting title. |
| 3 | **C. summarize-last-meeting** | Status returns 1 meeting whose `id` matches the seeded id; export markdown contains `> Privacy mode: local-first`, `> Cost: $…`, `## Questions & Answers`, the seeded question text, and the seeded answer text. |
| 4 | **D. PrivacyModeViolation (error)** | isError true, envelope text starts with `PrivacyModeViolation:`, spawn log file absent (proves gate fires before harness spawn closure). |
| 5 | **E. ContextNotFound (error)** | isError true, envelope text starts with `ContextNotFound:`, spawn log file absent. |

## 3. Acceptance Criteria Verdict

| AC | Status | Evidence |
|---|---|---|
| AC-1 — 3/3 deterministic runs | ✅ | 3 consecutive `bun run test tests/e2e/mcp-dispatch.e2e.test.ts` → 5/5 each, exits 0. |
| AC-2 — Scenario A asserts installed:false + downloadUrl | ✅ | `it("Scenario A: install-check…")` |
| AC-3 — Scenario B asserts spawn log + export markdown body | ✅ | `it("Scenario B: start-and-export…")` |
| AC-4 — Scenario C asserts markdown body contains Q + A + privacy | ✅ | `it("Scenario C: summarize-last-meeting…")` |
| AC-5 — Scenario D PrivacyModeViolation + empty spawn log | ✅ | `it("Scenario D (error): PrivacyModeViolation…")` |
| AC-6 — Scenario E ContextNotFound + empty spawn log | ✅ | `it("Scenario E (error): ContextNotFound…")` |
| AC-7 — Harness unit-tested | ✅ | 21 tests in `src/mcp/e2eHarness.test.ts` |
| AC-8 — Subprocess uses real shipped bin | ✅ | `bin.ts` diff shows single env-gated branch; spawn helper invokes `bun run <abs-path>/src/mcp/bin.ts`. |
| AC-9 — Discovery skipped in E2E mode | ✅ | `bin.ts` returns from `startE2EServer` before `tryRegisterDiscovery()`; spawn helper additionally pins `CLAUDE_BRIDGE_HOME`/`MCP_BRIDGE_HOME` to the per-scenario tmpdir as belt-and-brace. |
| AC-10 — Suite remains all-green | ✅ | 57 files / 900 tests / 0 fail. |

## 4. Process Rule 3 Checkboxes

- [x] **No regression Phase 1/2/3** — pre-T-4.10 baseline 55 / 874 tests still all green inside the new 57 / 900 total. Privacy module (`isSttProviderAllowed`), telemetry sink, persistClient — all untouched.
- [x] **MCP tool error handling proper (no panic on missing args; typed isError envelope)** — All 5 typed envelopes asserted by name in tests: `PrivacyModeViolation`, `ContextNotFound`, `MeetingNotFound` (status / stop / export), `installed:false` envelope (install). No subprocess crashes; the bin stays alive across error + happy calls in Scenario B (start fails on privacy → still alive for export call in subsequent scenario via per-scenario subprocess; harness unit test #14 + #18 + #21 also probe sequential calls inside one process).
- [x] **Deeplink URL parser safe (no injection; URLSearchParams only)** — Scenario B asserts `args[0]` matches `^meeting-copilot:\/\/start\?` produced by the production deeplink builder. The harness's spawn shim does NOT re-build the deeplink — it only logs whatever args the production handler passes through `spawn`. Scenario E exercises the path-traversal guard at the same code path.
- [x] **Privacy mode enforce at MCP layer (Local-only is NOT dispatchable to Cloud STT)** — Scenario D asserts the gate fires from a real subprocess JSON-RPC call. Spawn log absent proves the `child_process.spawn` closure in our harness is never invoked, which means the production gate fired BEFORE any spawn would have been invoked. Harness unit test #13 covers the same property at unit scope.
- [x] **Iframe embed CORS + auth check (allow-list + token TTL)** — N/A for T-4.10 directly (T-4.9 owns the embed surface). The E2E test does assert `start.uiUrl` shape (`http://127.0.0.1:7411/embed/transcript/<id>?token=<t>`), which is the contract the embed handler verifies in T-4.9. Discovery hook is not touched in E2E mode (AC-9).

Additional T-4.10-specific gates (loop driver Rule 3 plus T-4.10 risk register):

- [x] **Discovery never mutates real `~/.claude-bridge/config.json` from the E2E test** — bin.ts E2E branch returns BEFORE `tryRegisterDiscovery()`; the spawn helper also pins `CLAUDE_BRIDGE_HOME` to the per-scenario tmpdir.
- [x] **Subprocess teardown is reliable** — every scenario's `try/finally` calls `spawned.close()` which closes the client (which kills the child) AND `rmSync(workDir, recursive: force)`. No tmpdir leaks observed across 3 consecutive runs.
- [x] **Env var pollution avoided** — spawn helper builds env from `getDefaultEnvironment()` (SDK helper) + explicit fixture path; `process.env` is never mutated by the test.
- [x] **No real Tauri spawn / STT / socket bind escapes during the test** — Scenario B's spawn log records exactly one `open meeting-copilot://start?...` invocation, but the harness shim is the receiver; no actual `open(1)` runs because the production handler's `spawn` is overridden via the deps DI seam, not bypassed.

## 5. Determinism Log (AC-1 evidence)

```
=== Run 1 ===
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Duration  567ms

=== Run 2 ===
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Duration  553ms

=== Run 3 ===
 Test Files  1 passed (1)
      Tests  5 passed (5)
   Duration  559ms
```

Three consecutive runs, all exit 0, all 5/5 scenarios green. Spread = 14 ms over 553–567 ms — well within timeout headroom (30 s ceiling). No retries, no flakes.

## 6. Carry-Forward For Phase 4.x

- **Real subprocess + real Tauri spawn** — `PHASE-BROWSER-TEST.md` will run T-4.10's flow against a host with `Meeting Copilot.app` registered for the `meeting-copilot://` deeplink scheme.
- **Real helper-daemon socket** — when the daemon binary entrypoint lands in Phase 4.x, the harness's `dialSocket` overrides come out and the host run goes against the real UDS.
- **Real `persistClient.ts` SQLite-backed loader** — `bridge_meeting_export`'s production wiring lands in Phase 4.x; T-4.10's harness substitute (`fixture.snapshot`) is the contract that wiring must honor.
- **MCP dispatch from claude-bridge → Telegram round-trip** — out of scope for this repo (claude-bridge owns the dispatch + Telegram formatting); T-4.10 proves the meeting-copilot side of that flow.

## 7. Commit Plan

Single commit `test(e2e): T-4.10 Telegram→bridge→meeting-copilot harness` covering the 9 files in §1. No `git push`. Co-author footer per INDEX §"Sign-Off Trail".
