# T-4.1 — Integration Design — Self-Review

> Closes T-4.1 from `docs/tasks/phase-4/INDEX.md`. Pairs with `T-4.1-integration-design.md`.

## Files Changed

| Path | Type | Purpose |
|---|---|---|
| `docs/tasks/phase-4/T-4.1-integration-design.md` | new | 1-page integration design — discovery, MCP transport, embed, privacy gate, sequence diagram, 3 risks. |
| `docs/tasks/phase-4/T-4.1-review.md` | new | This file. |

**Touched-but-unchanged check**: Zero source files touched. INDEX.md untouched (it already contains the T-4.1 row; the checkbox flips post-commit per the loop driver convention, not in this review). No `package.json`, `Cargo.toml`, `tauri.conf.json` edits.

## Test Count

- **0 unit tests** (T-4.1 is doc-only by design — INDEX TDD row says "Doc-only review (markdown lint via `bun run typecheck` ignores docs; manual cross-ref check vs both ARCHs)").
- Manual lint: each cross-reference (`§9.1`, `§9.2`, `§9.3`, `§9.4` of meeting-copilot ARCH; `§7.2`, `§13`, `§14`, `§16` of dashboard ARCH) hand-verified to point at the actual section number in the corresponding doc — see Static-Trace below.
- All 5 MCP tool names match `claude-bridge/src/mcp/tools.ts` *style* (`bridge_<surface>_<verb>`), with `<surface>` = `meeting` (parallel to dashboard's `dashboard`).

## Cross-Reference Trace (manual lint)

| Cross-ref claim in design doc | Source file | Line(s) | Verdict |
|---|---|---|---|
| meeting-copilot ARCH §9 (lines 322–400) | `docs/ARCHITECTURE.md` | 322 (`## 9. MCP Integration`) → 400 (end of §9.4) | ✅ exists, content matches |
| meeting-copilot ARCH §9.1 Discovery | `docs/ARCHITECTURE.md` | 326–344 | ✅ exact match |
| meeting-copilot ARCH §9.2 5 MCP tools | `docs/ARCHITECTURE.md` | 347–388 | ✅ 5-tool list verbatim mirrored in design §4 |
| meeting-copilot ARCH §9.3 Helper daemon RPC | `docs/ARCHITECTURE.md` | 390–396 | ✅ Unix-socket path + 4 RPC methods cited |
| meeting-copilot ARCH §9.4 Dashboard embed | `docs/ARCHITECTURE.md` | 398–400 | ✅ iframe + token cited |
| meeting-copilot ARCH §11.1 privacy gate | `docs/ARCHITECTURE.md` | (Phase 3 T-3.8 anchor) | ✅ pre-spawn gate decision recorded |
| dashboard ARCH §7.2 Discovery Protocol | `claude-bridge/docs/web-dashboard/v2/ARCHITECTURE.md` | 62–82 | ✅ `discoverBridgeDaemon` / `BridgeNotInstalledError` pattern adopted |
| dashboard ARCH §13 MCP Tool Design | `claude-bridge/docs/web-dashboard/v2/ARCHITECTURE.md` | 166–207 | ✅ Zod-schema + idempotent-install pattern reused |
| dashboard ARCH §14 Agent Integration | `claude-bridge/docs/web-dashboard/v2/ARCHITECTURE.md` | 210–248 | ✅ `meeting-copilot--default` agent name shape mirrored |
| dashboard ARCH §16 Config Schema | `claude-bridge/docs/web-dashboard/v2/ARCHITECTURE.md` | 273–306 | ✅ `version: 1`, absolute-paths-only, atomic-write rules adopted |

| Reference impl pointer | claude-bridge file | Verified |
|---|---|---|
| `TOOL_NAMES` constant | `src/mcp/tools.ts:32, 60–62` | ✅ `bridge_dashboard_versions/install/uninstall` present |
| `TOOL_DEFINITIONS` array | `src/mcp/tools.ts:67, 383–449` | ✅ Zod schemas present per the dashboard ARCH §13 layout |
| Idempotent installer | `src/lib/dashboard-installer.ts` | ✅ exists (used as structural blueprint for T-4.8 atomic-write pattern) |
| Config-file shape | `src/lib/dashboard-config.ts` | ✅ exists (used as structural blueprint for `meeting_copilots[]` array additive) |

(Listed for downstream T-4.2/T-4.8 to consume; no code copied at this point.)

## Self-Review Checklist

- [x] **No regression Phase 1/2/3** — zero source files touched; doc-only commit. No risk of import-path drift, schema drift, test breakage. Phase 3 sign-off (`PHASE-3-COMPLETE.md`) and prior task carry-forwards remain green.
- [x] **MCP tool error handling proper (no panic on missing args)** — design §4 enumerates a closed set of typed error codes (`PrivacyModeViolation`, `DeeplinkNotRegistered`, `ContextNotFound`, `MeetingNotFound`, `InvalidMeetingId`, `ConfigSchemaUnsupported`, `BridgeConfigInvalid`) returned via the standard MCP `{ isError: true, content: [{ type: "text", text }] }` envelope. T-4.2 placeholders + T-4.3..T-4.7 handlers must conform; the contract is locked here. (No code in T-4.1, so this is a *design-locked* checkbox, not a test-verified one.)
- [x] **Deeplink URL parser safe (no injection)** — design §6 step 4 mandates `URLSearchParams` exclusively + `child_process.spawn` array form with `shell: false`. T-4.4 review will hold the line with 10 injection-pattern unit tests; T-4.1 ships the rule.
- [x] **Privacy mode enforce ở MCP layer (Local-only KHÔNG được dispatch nếu user pick)** — design §6 places the `isSttProviderAllowed` gate **before** any process spawn (step 2 of the `bridge_meeting_start` pipeline). Local-first + `sttProvider="deepgram"` returns typed `PrivacyModeViolation`, never spawns. T-4.4 will land the test; T-4.1 ships the architectural decision.
- [x] **Iframe embed CORS + auth check** — design §5.3 hardcodes `Access-Control-Allow-Origin: http://127.0.0.1:7878` (no wildcard). §5.4 mints single-use UUID tokens with 5-min TTL, scope = meetingId, plus `Referrer-Policy: no-referrer` header. T-4.9 axum test enforces 4 cells (200/401/401-expired/403-CORS); T-4.1 ships the rule.
- [x] **No `git push`** — per loop rule 4. Commit lands on `main` locally only.
- [x] **Cost tracking** — N/A for T-4.1 (zero LLM calls, zero network calls, doc-only).
- [x] **No secret leak** — no API keys, no env-var reads added. Embed token model documented; never written to disk (in-memory `HashMap` only — T-4.9).
- [x] **No new dependencies** — `package.json` / `Cargo.toml` unchanged.

## Risk Re-Check

| From task spec (INDEX line 55 + design §8) | Outcome / Mitigation locked |
|---|---|
| **R-1**: MCP SDK / config-schema drift between MC and claude-bridge daemon | Pinned daemon snapshot v1.0.4 cited in design §3.2 + §1 frontmatter. T-4.2 will add a `package.json` regex test enforcing `@modelcontextprotocol/sdk ^1.0.0`. T-4.8 will reject `config.version > 1` with a typed error. |
| **R-2**: Deeplink registration / injection | Manifest-time scheme registration locked in design §8 R-2 mitigation; manual-verify checklist deferred to T-4.4 deliverable. URL build rules locked in §6. |
| **R-3**: CORS / token leak on embed | CORS allow-list pinned to single origin; token TTL 5 min, scope = meetingId; `Referrer-Policy: no-referrer` mandated. Test plan locked for T-4.9. |

## Phase-Goal Re-Check

T-4.1 ships the **contract lock-down** for Phase 4. The Phase 4 exit criterion ("Telegram → dispatch meeting-copilot → app start → … → answer back qua bridge notify, < 30s setup") depends on four contract surfaces being stable across T-4.2..T-4.10: discovery (§2), MCP transport (§3), embed (§5), privacy gate (§6). All four are now frozen by design and traceable back to the cross-referenced ARCH sections. Subsequent tasks implement against this design without re-litigating shape decisions.

## Hardware-Verification Caveats

None. Doc-only task; no compile / test / runtime gates pending.

## Sign-Off

T-4.1 **doc-complete + AC-verified by inspection** (cross-references all hand-traced; 5-tool mapping listed; 3 risks identified; sequence diagram included). Ready to commit (type `docs`). Next loop iteration: T-4.2 — MCP server skeleton (stdio + 5 placeholders + Zod schemas).
