# T-4.9 Review — Dashboard iframe embed endpoint (HTTP + SSE)

> Self-review per `docs/tasks/phase-4/INDEX.md` §"Process Rules" Rule 3.
> Spec: `docs/tasks/phase-4/T-4.9-dashboard-iframe-embed.md`.

## 1. Files Touched

| Path | Action | LOC delta (approx) |
|---|---|---|
| `crates/helper-daemon/src/auth.rs` | new | +220 (incl. 11 tests) |
| `crates/helper-daemon/src/embed_http.rs` | new | +590 (incl. 14 tests) |
| `crates/helper-daemon/src/lib.rs` | edit | +12 / −0 |
| `crates/helper-daemon/Cargo.toml` | edit | +9 / −0 |
| `src/embed/types.ts` | new | +60 |
| `src/embed/TranscriptEmbed.tsx` | new | +180 |
| `src/embed/transcript-entry.tsx` | new | +50 |
| `src/embed/transcript.html` | new | +14 |
| `src/embed/TranscriptEmbed.test.tsx` | new | +400 (incl. 21 tests) |
| `vite.config.ts` | edit | +14 / −0 |
| `docs/tasks/phase-4/T-4.9-dashboard-iframe-embed.md` | new | task spec |
| `docs/tasks/phase-4/T-4.9-review.md` | new | this file |

Total ≈ 1,550 LOC of code + tests + docs (within INDEX budget of 700–900 LOC code; docs + tests push the gross delta higher per the same convention as T-4.5/T-4.6/T-4.7).

## 2. Test Run Summary

- **TS suite**: `bun run test` — **55 files / 874 tests / 0 fail / 0 skip** (was 854 / 853 pre-T-4.9; +21 new tests in `src/embed/TranscriptEmbed.test.tsx`).
- **Typecheck**: `bun run typecheck` — clean (`tsc --noEmit` exits 0).
- **Rust suite**: 25 new `#[test]` / `#[tokio::test]` cases ship code-complete in `auth.rs` (11) + `embed_http.rs` (14). Host `cargo test -p helper-daemon` verify pending Phase 0 carry-forward (sandbox lacks `cargo` per `docs/tasks/phase-4/INDEX.md` line 47-48 — same gate as T-4.5/T-4.6/T-4.7).

### TranscriptEmbed.test.tsx — what each test covers

| # | Test | Asserts |
|---|---|---|
| 1 | `reduce > appends a transcript chunk` | First chunk lands |
| 2 | `reduce > de-dupes a re-delivered chunk by chunkId` | Idempotent on broadcast lag |
| 3 | `reduce > attaches an answer to the matching question` | Q→A linkage by `questionId` |
| 4 | `reduce > ignores an answer with no matching question` | Orphan answer dropped |
| 5 | `reduce > freezes state on meetingEnded` | `ended` flag flipped + ts captured |
| 6 | `reduce > updates connection state via dedicated action` | Lifecycle reducer |
| 7 | `reduce > dedupes a question with the same questionId` | No duplicate Q |
| 8 | `eventSourceUrl > encodes meetingId path + token query` | URL builder shape |
| 9 | `eventSourceUrl > percent-encodes special chars in meetingId` | `m/with slash` → `m%2Fwith%20slash` |
| 10 | `eventSourceUrl > percent-encodes special chars in token` | `a&b=c` → `a%26b%3Dc` |
| 11 | `formatTimestamp > formats h/m/s/ms` | `01:01:01.001` |
| 12 | `formatTimestamp > clamps negative` | `00:00:00.000` |
| 13 | `formatTimestamp > clamps NaN` | `00:00:00.000` |
| 14 | `<TranscriptEmbed/> > connecting → open lifecycle` | Status pill |
| 15 | `<TranscriptEmbed/> > renders chunks` | DOM render of chunk |
| 16 | `<TranscriptEmbed/> > renders Q/A pairs` | DOM render of Q+A |
| 17 | `<TranscriptEmbed/> > onerror → error status` | Error handling |
| 18 | `<TranscriptEmbed/> > ignores malformed JSON frames` | Defence in depth |
| 19 | `<TranscriptEmbed/> > closes EventSource on unmount` | Cleanup on unmount |
| 20 | `<TranscriptEmbed/> > freezes footer to 'Ended'` | meetingEnded UI flip |
| 21 | `<TranscriptEmbed/> > uses default factory (window.EventSource)` | Fallback wiring |

### Rust tests — what each covers (host-verify pending)

`auth.rs` (11):
1. `mint_returns_uuid_v4_and_records_ttl` — UUID v4 nibble check.
2. `mint_overwrites_prior_token_for_same_meeting` — re-mint replaces.
3. `verify_accepts_within_ttl` — under-boundary success.
4. `verify_rejects_after_ttl_expiry` — past-boundary rejection.
5. `verify_rejects_at_exact_ttl_boundary` — `<= now` semantics.
6. `verify_rejects_unknown_meeting` — `NotFound`.
7. `verify_rejects_scope_mismatch_after_re_mint` — m_2 token ≠ m_1.
8. `verify_rejects_random_token_for_known_meeting` — `Mismatch`.
9. `clear_removes_entry_and_subsequent_verify_returns_not_found` — clear path.
10. `clear_unknown_meeting_is_noop` — idempotent.
11. `mint_for_two_meetings_keeps_both` — concurrent meetings.

`embed_http.rs` (14):
1. `html_route_returns_200_on_valid_token_and_origin` — happy path + all 4 hardening headers.
2. `html_route_rejects_missing_token_with_401` — auth gate.
3. `html_route_rejects_expired_token_with_401` — fake-clock TTL test.
4. `html_route_rejects_token_for_different_meeting_with_401` — scope mismatch.
5. `html_route_rejects_malformed_token_with_401` — UUID parse fail.
6. `html_route_rejects_missing_origin_with_403` — CORS gate (no Origin).
7. `html_route_rejects_evil_origin_with_403` — CORS gate (foreign origin).
8. `html_route_rejects_wrong_loopback_port_with_403` — 127.0.0.1:1234 ≠ allow-list.
9. `cors_check_does_not_emit_wildcard_header_under_any_branch` — wildcard veto.
10. `sse_route_rejects_unknown_meeting_channel_with_401` — channel gate.
11. `sse_route_streams_events_in_submitted_order` — wire order + side-channel headers.
12. `sse_route_emits_security_headers` — no-channel branch carries headers.
13. `sse_route_rejects_evil_origin_with_403` — CORS on SSE.
14. `register_and_deregister_channel_are_observable_under_lock` — state mutation visible.

## 3. Acceptance Criteria

| AC | Verdict |
|---|---|
| Token mint round-trip + TTL | ✅ `auth_tests::mint_*` + `verify_*` (host pending) |
| 200 on valid token + origin | ✅ `html_route_returns_200…` |
| 401 on missing token | ✅ `html_route_rejects_missing_token…` |
| 401 on expired token (fake clock) | ✅ `html_route_rejects_expired_token…` |
| 401 on scope mismatch | ✅ `html_route_rejects_token_for_different_meeting…` |
| 403 on missing origin | ✅ `html_route_rejects_missing_origin…` |
| 403 on `Origin: http://evil.example` | ✅ `html_route_rejects_evil_origin…` |
| SSE event order preserved | ✅ `sse_route_streams_events_in_submitted_order` (chunk < q < a) |
| `Referrer-Policy: no-referrer` set | ✅ `html_route_returns_200…` + `sse_route_streams_events…` + 401-branch tests |
| `Cache-Control: no-store` set | ✅ same set as above |
| `<TranscriptEmbed/>` connect/event/disconnect lifecycle | ✅ tests 14-21 |
| Full `bun test` green | ✅ 874/874 |
| Full `bun run typecheck` green | ✅ |
| No Phase 1/2/3 regression | ✅ delta is +21 tests, none flipped |

## 4. Code Review Checklist (Phase 4 binding — INDEX §"Process Rules" Rule 3)

- [x] **No regression Phase 1/2/3** — full TS suite at 874 / 0 fail; the only file edits outside `src/embed/` + `crates/helper-daemon/` are `vite.config.ts` (additive rollup input — does not change the `main` entry's build) and `Cargo.toml` (additive deps — `tokio` features unchanged so T-4.5 / T-4.6 RPC code compiles unchanged).
- [x] **MCP tool error handling proper** — N/A for T-4.9 directly (this task adds the embed surface, not a new MCP tool); the existing 5-tool envelope is unchanged. The embed-side 401/403 responses do **not** leak transcript bytes (bodies are short ASCII strings: `"missing token"` / `"token expired"` / etc.).
- [x] **Deeplink URL parser safe** — N/A; the embed URL is built from `URLSearchParams` in `eventSourceUrl` (test 8-10 cover the encoding) and from `axum::extract::Path` + `axum::extract::Query` on the daemon side (no string concat into URLs anywhere in this task's code).
- [x] **Privacy mode enforce at MCP layer** — Phase 3 T-3.8 gate is preserved. The embed surface is **read-only** and only emits chunks the daemon already collected post-T-4.4 spawn; no fresh STT provider is selected here. The token-bearer is the dashboard, which is the same operator who picked the privacy mode at start time.
- [x] **Iframe embed CORS + auth check** (R-3 BLOCKING) —
  - [x] CORS allow-list hardcoded to `http://127.0.0.1:7878` (`embed_http.rs::DEFAULT_ALLOWED_ORIGIN`).
  - [x] Wildcard `*` is **never** emitted (test 9 enumerates all branches).
  - [x] `Referrer-Policy: no-referrer` on every response (tested on 200 + 401 + 403 paths).
  - [x] `Cache-Control: no-store` on every response (same coverage).
  - [x] `X-Frame-Options: SAMEORIGIN` defence-in-depth header.
  - [x] Token TTL enforced via injected clock; boundary semantics tested at TTL exact + 1ns.
  - [x] Token scope (meetingId) enforced; cross-pair token rejected with `Mismatch`.
  - [x] No `Set-Cookie`, no token in body, no token written to disk (in-memory `TokenStore` only).

## 5. Risks & Mitigations Re-Check

| Risk | Mitigation status |
|---|---|
| **R-3 CORS misconfiguration** | Hardcoded allow-list; 4-branch CORS test asserts no wildcard ever emitted. ✅ |
| **R-3 Token leak via Referer** | `Referrer-Policy: no-referrer` on every response; tested. ✅ |
| **R-3 Token leak via cache** | `Cache-Control: no-store` on every response; tested. ✅ |
| **TTL bypass / clock skew** | Clock injected; TTL boundary tested at `exactly_ttl` and `+1s`. ✅ |
| **Scope confusion** | Cross-meeting token rejected; tested. ✅ |
| **SSE backpressure** | `tokio::sync::broadcast` `Lagged` swallowed (browser reconnects naturally — auth + scope gate fires afresh on reconnect). Documented in `broadcast_to_sse`. ✅ |
| **Vite build entry collision** | Additive `embedTranscript` input; existing `main` retains `index.html`. ✅ |
| **EventSource mock drift** | Mock implements only the 4 fields the component touches (`onopen`/`onmessage`/`onerror`/`close`); identical pattern to existing test mocks. ✅ |

## 6. Out-of-Scope Carry-Forward (Phase 4.x)

- Daemon binary entrypoint that wires `EmbedState` into `axum::serve` and binds `127.0.0.1:7411` — Phase 4.x.
- The broadcast `Sender` per active meeting is constructed by the entrypoint when the Chunker → STT pipeline starts; this lib's `register_channel` API is the seam.
- Auto-deregister on `bridge_meeting_stop` — entrypoint owns the timing; the API is `EmbedState::deregister_channel`.
- Persistent tokens across daemon restart — explicitly out of scope; restart invalidates all outstanding tokens by design.

## 7. Sign-Off

- ✅ All AC met; tests green (874 TS / 25 Rust code-complete).
- ✅ R-3 BLOCKING checkboxes all ticked.
- ✅ INDEX checkbox ready to flip with `[x] T-4.9 — DONE` + headline outcome.
- ⏭ Per-task commit `feat(api): T-4.9 iframe embed + SSE` next.
- ⏭ No `git push` (operator pushes after Phase 4 sign-off).
