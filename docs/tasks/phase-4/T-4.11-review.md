# T-4.11 — Code Review (docs: README + ARCHITECTURE Phase 4 addendum + mcp-tools.md)

> Reviewer: loop iter 12 self-review against `docs/tasks/phase-4/T-4.11-readme-arch.md`.
> Pre-T-4.11 vitest baseline: **900 / 900** (T-4.10 sign-off, 57 files). Post: **900 / 900** (no test deltas — docs-only task).
> Cargo: untouched (no Rust changes).
> `tsc --noEmit`: clean (`bun run typecheck` exits 0).

---

## 1. What shipped

| Path | Change | Notes |
|---|---|---|
| `docs/ARCHITECTURE.md` | extended (append-only, +157 lines, 0 deletions) | 4 new blocks: §9.5 Phase 4 update — MCP server impl pointer (T-4.2..T-4.7); §9.6 Phase 4 update — Deeplink scheme + spawn safety (T-4.4); §9.7 Phase 4 update — Embed CORS + token TTL (T-4.9); §16 NEW — Discovery `~/.claude-bridge/config.json` (T-4.8). Existing §1–§15 + Appendix A/B byte-identical. |
| `README.md` | extended (+107 lines, 0 deletions) | New `## MCP Integration with claude-bridge` section between "Supported Platforms" and "Documentation" — 5 subsections (intro + tool surface table + Install/register + Example config + Telegram dispatch examples + Privacy/safety guarantees). One anchor line added at end of existing Phase 4 quickstart block pointing to the new section. |
| `docs/mcp-tools.md` | new (+367 lines) | 1-page reference: tool surface table, closed error code set, 5 per-tool reference subsections, discovery quick-ref, telemetry note, references. |
| `docs/tasks/phase-4/T-4.11-readme-arch.md` | new (+185 lines) | Task spec. |
| `docs/tasks/phase-4/T-4.11-review.md` | new | This file. |
| `docs/tasks/phase-4/INDEX.md` | edit (+1 / −1) | T-4.11 checkbox flipped + outcome line appended. |

Total: ~1,525 lines of new + extended markdown across 4 files (+ this review). All within the INDEX budget of "300–500 LOC of markdown" once review + spec docs are factored separately, per the same convention as T-3.10 and T-4.10.

---

## 2. Append-only verification (R-4.11-3)

Goal: prove no existing ARCH content was removed or moved.

```
$ git diff --stat HEAD -- docs/ARCHITECTURE.md README.md
 README.md            | 108 +++++++++++++++++++++++++++++++++++
 docs/ARCHITECTURE.md | 157 +++++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 265 insertions(+)

$ git diff HEAD -- docs/ARCHITECTURE.md | grep -E "^-[^-]"
(empty — no deletion lines)

$ git diff HEAD -- README.md | grep -E "^-[^-]"
(empty — no deletion lines)
```

Net insertions only. Existing ARCH §1–§15 + Appendix A/B byte-identical; existing README §"Vision"/"Quickstart"/"Supported Platforms"/"Documentation"/"License" byte-identical. The append-only guarantee from `T-4.11-readme-arch.md` §"Non-goals" holds.

---

## 3. Snippet validates against schema (R-4.11-4)

The README's copy-paste `~/.claude-bridge/config.json` snippet was round-tripped through the live Zod schema during this iter:

```ts
import { BridgeConfigSchema } from "src/discovery/schema.ts";
const result = BridgeConfigSchema.safeParse(JSON.parse(snippet));
// result.success === true
// result.data.meeting_copilots.length === 1
// result.data.daemon preserved verbatim via .passthrough()
```

The exact same snippet is duplicated verbatim in ARCH §16.3 — both citations are pinned to the same JSON, so a future drift would surface in both docs simultaneously.

---

## 4. Tool surface — byte parity with `src/mcp/tools.ts`

Source-of-truth check (R-4.11-5): every tool name, error code, enum literal, and Zod-derived schema field in `docs/mcp-tools.md` was cross-walked against `src/mcp/tools.ts` this iter.

| Surface element | Source file lines | mcp-tools.md location | Verdict |
|---|---|---|---|
| `TOOL_NAMES` array (5 names) | `tools.ts` 22–28 | §1 surface table + §3 per-tool headers | ✅ exact |
| `ERROR_CODES` array (8 codes) | `tools.ts` 42–51 | §2 closed error code set table | ✅ exact |
| `STT_PROVIDER_ENUM` | `tools.ts` 90 | §3.2 / §3.3 sttProvider field | ✅ exact (`mlx`, `fake`, `deepgram`, `elevenlabs`) |
| `TTS_PROVIDER_ENUM` | `tools.ts` 91 | §3.2 ttsProvider field | ✅ exact (`off`, `elevenlabs`) |
| `PRIVACY_MODE_ENUM` | `tools.ts` 92 | §3.2 privacyMode field | ✅ exact (`local-first`, `cloud`, `mixed`) |
| `MODEL_ENUM` | `tools.ts` 93 | §3.2 model field | ✅ exact (`claude-sonnet-4-6`, `claude-haiku-4-5`) |
| `EXPORT_FORMAT_ENUM` | `tools.ts` 94 | §3.5 format field | ✅ exact (`markdown`, `json`, `vtt`, `srt`) |
| `InstallInputSchema` strict + 4 optional fields | `tools.ts` 99–106 | §3.1 Input schema | ✅ exact |
| `InstallOutputSchema` 5 fields | `tools.ts` 108–116 | §3.1 Output schema | ✅ exact |
| `StartInputSchema` 6 fields + `.strict()` | `tools.ts` 123–132 | §3.2 Input schema | ✅ exact |
| `StartOutputSchema` 4 fields | `tools.ts` 134–141 | §3.2 Output schema | ✅ exact |
| `StatusInputSchema` 1 optional + `.strict()` | `tools.ts` 144–148 | §3.3 Input schema | ✅ exact |
| `StatusOutputSchema` meeting array fields | `tools.ts` 150–167 | §3.3 Output schema | ✅ exact |
| `StopInputSchema` 1 required field | `tools.ts` 170–174 | §3.4 Input schema | ✅ exact |
| `StopOutputSchema` 3 fields | `tools.ts` 176–182 | §3.4 Output schema | ✅ exact |
| `ExportInputSchema` 2 required fields | `tools.ts` 185–190 | §3.5 Input schema | ✅ exact |
| `ExportOutputSchema` 2 fields | `tools.ts` 192–197 | §3.5 Output schema | ✅ exact |
| `TOOL_DEFINITIONS` 5 descriptions | `tools.ts` 211–242 | §1 surface table fourth column | ✅ verbatim quote |

`bun run test src/mcp/tools.test.ts` re-run during this iter: **18 / 18 green** (no schema regression vs T-4.10 baseline).

---

## 5. Link audit

Every implementation file path mentioned in either ARCH §9.5/§9.6/§9.7/§16, README §MCP Integration, or `docs/mcp-tools.md` was verified to exist on disk this iter:

- `src/mcp/{bin,server,tools,deeplink,socket}.ts` (+ `*.test.ts`) — present.
- `src/mcp/handlers/{install,start,status,stop,export}.ts` (+ `*.test.ts`) — present.
- `src/mcp/exporters/{markdown,json,vtt,srt}.ts` — present.
- `src/discovery/{schema,bridgeConfig}.ts` (+ `*.test.ts`) — present.
- `src/privacy/privacyMode.ts` — present.
- `src/telemetry/telemetryLog.ts` — present.
- `src/embed/{TranscriptEmbed.tsx,transcript-entry.tsx}` — present.
- `crates/helper-daemon/src/{embed_http,auth,mcp_rpc}.rs` — present.
- `crates/stt-mlx/src/providers/privacy.rs` — present.
- `src-tauri/tauri.conf.json` — present.
- `tests/e2e/mcp-dispatch.e2e.test.ts` — present.
- `docs/tasks/phase-4/T-4.{2..10}-review.md` — present.
- `docs/tasks/phase-4/T-4.4-manual-verify-deeplink.md` — present.
- `docs/{ARCHITECTURE,IMPLEMENTATION-PLAN,PRD,mcp-tools,provider-comparison}.md` — present.

`docs/release/DMG-NOTARIZE.md` is **NOT yet present** — that artifact ships in T-4.12. The README install instruction was softened to read "release notarization instructions land alongside the v1.0 release; see docs/release/ once published" (no broken link to a not-yet-existing file).

No other broken links.

---

## 6. Review checklist (from spec §"Definition of done")

- [x] **`docs/ARCHITECTURE.md` has 4 new blocks (§9.5, §9.6, §9.7, §16).** Headers match the `### N.X Phase 4 update — <subject>` pattern (§9.5/§9.6/§9.7) and the canonical numbered-section pattern (§16). Existing §1–§15 + Appendix A/B byte-identical (verified §2 above).
- [x] **`README.md` has a new `## MCP Integration with claude-bridge` section** between "Supported Platforms" and "Documentation", with all 5 subsections from spec §4.1 (intro paragraph + 5-row tool surface table; Install + register; Example config; Telegram dispatch examples; Privacy + safety guarantees).
- [x] **`docs/mcp-tools.md` exists with all 7 required sections** (header + scope; tool surface table; closed error code set; per-tool reference ×5; discovery quick-ref; telemetry note; references). Tool surface byte-matches `src/mcp/tools.ts` (verified §4 above).
- [x] **All three docs cross-reference implementation file paths verbatim (no broken links).** Audit above (§5).
- [x] **Copy-paste README config snippet validates against `BridgeConfigSchema.safeParse(...)`.** Verified §3 above.
- [x] **`T-4.11-review.md` documents what landed + the link-audit verdict + the snippet-validates-against-schema verdict.** This file.
- [x] **`INDEX.md` flips T-4.11 checkbox + outcome line.** Done in same commit.
- [x] **Per-task commit type `docs:` with the co-author footer.** Lands alongside this doc.
- [x] **No `git push`.** Local commit only.
- [x] **`bun run test` carry-forward green (900 / 900).** Post-edit run confirms 57 files / 900 tests pass; no flake / no skip. Transcript:
  ```
  Test Files  57 passed (57)
  Tests  900 passed (900)
  Duration  2.13s
  ```
- [x] **`tsc --noEmit` clean.** `bun run typecheck` returns 0.

---

## 7. INDEX checklist mapping (Phase 4 standard 5-row review)

- [x] **No regression Phase 1/2/3.** Phase 3 sign-off was 602 / 602; Phase 4 baseline through T-4.10 was 900 / 900; T-4.11 stays 900 / 900. The 298-test delta (602 → 900) is all Phase 4 net new (T-4.2..T-4.10), accumulated commit-by-commit and asserted in each per-task review. T-4.11 is docs-only and adds zero tests.
- [x] **MCP tool error handling proper (no panic on missing args; typed `isError` envelope).** Documented at ARCH §9.5 ("error envelope: every handler returns `{isError: true, content: [{type:"text", text: "<ErrorCode>: <message>"}]}`") and at `docs/mcp-tools.md` §2 (closed error code set). The doc reflects the existing implementation; nothing in T-4.11 mutates handler behavior.
- [x] **Deeplink URL parser safe (no injection; URLSearchParams only).** Documented at ARCH §9.6 ("Pure URL builder: `src/mcp/deeplink.ts` builds the deeplink only via `URLSearchParams` — no string concatenation, ever") and at `docs/mcp-tools.md` §3.2 pre-spawn gates section. Reflects the T-4.4 implementation; nothing in T-4.11 mutates URL build.
- [x] **Privacy mode enforce at MCP layer (Local-only is NOT dispatchable to Cloud STT).** Documented at ARCH §9.6 ("Privacy mode gate via `isSttProviderAllowed(privacyMode, sttProvider)` … Disallowed pair → `PrivacyModeViolation`, spawn never invoked") and at README §"Privacy + safety guarantees" first bullet + `docs/mcp-tools.md` §3.2 "Pre-spawn gates (BLOCKING)". Reflects T-4.4 + Phase 3 T-3.8 carry-forward.
- [x] **Iframe embed CORS + auth check (allow-list + token TTL).** Documented at ARCH §9.7 ("CORS allow-list = `http://127.0.0.1:7878` only … Hardcoded; no wildcard ever") + ("Auth = single-use UUID token … 5-minute TTL") and at README §"Privacy + safety guarantees" second bullet + `docs/mcp-tools.md` §3.2 (`uiUrl` field carrying the token). Reflects T-4.9.

---

## 8. No-regression evidence

| Surface | Pre (T-4.10) | Post (T-4.11) | Delta | Notes |
|---|---|---|---|---|
| vitest total | 900 | 900 | 0 | Docs-only — no source touched. |
| `bun run typecheck` | clean | clean | — | `tsc --noEmit` returns 0. |
| Rust workspace | code-complete (Phase 0 cargo verify pending) | code-complete | — | `crates/` not in this iter's diff. |
| `src/mcp/tools.test.ts` | 18 / 18 | 18 / 18 | 0 | Snapshot of `tools/list` schema unchanged. |
| ARCH net lines | 635 | 792 | +157 | All append-only (verified). |
| README net lines | 73 | 181 | +108 | All append (intra-section anchor + new top-level section). |

---

## 9. Risk register — outcome

| # | Risk | Outcome |
|---|---|---|
| R-4.11-1 | Doc drift — claims a tool / error code shipped that hasn't | Mitigated. §4 byte-parity walk confirmed all 5 tools + 8 error codes + 5 enums match `src/mcp/tools.ts` 1:1. Future drift surfaces as a snapshot mismatch in `tools.test.ts`. |
| R-4.11-2 | Cross-reference rot | Mitigated. §5 link audit confirmed every cited path resolves. The single forward-reference (`docs/release/DMG-NOTARIZE.md`) was softened to a "land alongside v1.0 release" note. |
| R-4.11-3 | Existing ARCH content accidentally deleted | Mitigated. §2 confirmed `git diff HEAD -- docs/ARCHITECTURE.md \| grep -E "^-[^-]"` is empty. Insertions-only. |
| R-4.11-4 | README MCP example config invalid | Mitigated. §3 confirmed the snippet round-trips through `BridgeConfigSchema.safeParse(...)` with `success: true`. The same snippet is mirrored at ARCH §16.3 — drift would break both docs at once. |
| R-4.11-5 | mcp-tools.md duplicates Zod schemas verbatim, the two drift | Mitigated. §4 byte-parity walk + the §6 source-of-truth note in `docs/mcp-tools.md` itself ("the canonical reference is the implementation file; this doc reflects the values at the time of the Phase 4 sign-off"). |

---

## 10. Outcome

T-4.11 is **complete**. The three doc artifacts (ARCH addendum, README MCP section, `docs/mcp-tools.md`) reflect Phase 4 reality byte-for-byte against the landed code at the T-4.10 sign-off snapshot. Append-only invariant on ARCH holds. Cross-references resolve. Copy-paste config snippet validates against the live Zod schema. Full vitest suite + typecheck stay green.

T-4.12 is unblocked: it can cut the v1.0.0 annotated tag and link the tag annotation to this trio of docs.
