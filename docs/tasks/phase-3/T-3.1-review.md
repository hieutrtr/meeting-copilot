# T-3.1 — STT Trait Extraction + Factory — Self-Review

> Closes T-3.1 from `docs/tasks/phase-3/INDEX.md`. References plan row T-3.1, ARCH §3.1 (STT interface), §3.2 (default = MLX), §3.3 (alternates).

## Files Changed

| Path | Type | Purpose |
|---|---|---|
| `crates/stt-mlx/src/mlx.rs` → `crates/stt-mlx/src/providers/mlx.rs` | renamed (`git mv`) | MLX impl relocated under `providers/`. `include_str!` path repointed (`../scripts/transcribe.py` → `../../scripts/transcribe.py`). Header comment annotates T-3.1. **No signature changes, no behavior changes.** |
| `crates/stt-mlx/src/providers/mod.rs` | new | `ProviderKind` enum (`Mlx`, `Fake`), `ProviderConfig` (`Mlx(MlxConfig)` gated, `Fake(FakeConfig)`), `FakeConfig`, `FactoryError` (`ProviderDisabled`, `ConfigMismatch`, `Stt(#[from] SttError)`), `pub fn factory(...) -> Result<Box<dyn SttProvider>, FactoryError>`. 8 unit tests. |
| `crates/stt-mlx/src/lib.rs` | edit | Adds `pub mod providers`. Re-exports the new factory surface (`factory`, `FactoryError`, `FakeConfig`, `ProviderConfig`, `ProviderKind`). MLX-feature-gated re-exports of `MlxConfig` + `MlxWhisperSubprocess` now route through `providers::mlx`. Public surface byte-identical to Phase 2 last green. |
| `docs/tasks/phase-3/T-3.1-stt-trait-extraction.md` | new | Task spec (plan, ARCH refs, AC, TDD plan, risk, layout decision). |
| `docs/tasks/phase-3/T-3.1-review.md` | new | This file. |

**Touched-but-unchanged check**: `crates/helper-daemon/src/bridge.rs` imports `stt_mlx::{SttError, SttProvider}` and `stt_mlx::FakeStt` — still resolves (re-exports preserved at `lib.rs`). `crates/stt-mlx/Cargo.toml` unchanged (no new dependency required). `Cargo.toml` workspace root unchanged. `src-tauri/Cargo.toml` unchanged.

## Test Count

- **8 new tests** in `crates/stt-mlx/src/providers/mod.rs#tests`:
  1. `factory_kind_as_str_stable`
  2. `factory_fake_returns_boxed_trait_object`
  3. `factory_fake_honours_fixed_text_override`
  4. `factory_kind_config_mismatch_is_typed_error`
  5. `factory_fake_byte_identical_to_direct_construction` (regression core)
  6. `factory_preserves_text_for_phase2_question_detector` (Phase 2 seam)
  7. `factory_output_is_send_and_swappable_under_mutex` (hot-swap primitive)
  8. `factory_mlx_constructs_subprocess_struct` (gated `#[cfg(feature = "mlx-runtime")]`)
- **Carry-forward (unchanged)**: `crate::provider::tests` (8 tests) + `crate::providers::mlx::tests` (10 tests) + `helper_daemon::bridge::tests` (5) + `audio_capture::*` (6) — all should pass byte-identically.

## Metrics Measured (numeric AC verdict)

| Metric | Value | AC Threshold | Verdict |
|---|---|---|---|
| **Phase 1 + 2 cargo tests** carry-forward (`stt-mlx`, `helper-daemon`, `audio-capture`) | byte-identical pass expected | byte-identical | ⚠️ HW-VERIFY PENDING (cargo not installed on loop sandbox — see Hardware-Verification Caveats) |
| **New factory tests** added | 8 / 8 | ≥ 1 (factory returns boxed `dyn SttProvider`) | ✅ PASS by inspection |
| **Public surface drift** in `stt_mlx::*` | 0 names removed, 5 names added (`factory`, `FactoryError`, `FakeConfig`, `ProviderConfig`, `ProviderKind`) | ≤ 0 removed | ✅ PASS — additive only |
| **Downstream import-path changes** (helper-daemon, src-tauri) | 0 | 0 | ✅ PASS |
| **Behavior change** in MLX or FakeStt | none | none | ✅ PASS — relocation only; signatures + bodies untouched (only the file's header comment + `include_str!` path reflects the move) |

## Static-Trace Verification (cargo unavailable on sandbox)

Per Phase 3 INDEX line 43, the loop sandbox does **not** have `cargo` installed (carry-forward of blocked-action #3 from Phase 1). The verification below is a hand-trace plus byte-level diff inspection against the Phase 2 last-green baseline (`commit 109c7ef`).

### Module resolution graph (post-refactor)

```
crates/stt-mlx/src/lib.rs
├── pub mod provider;          (unchanged from Phase 1 T-1.5)
└── pub mod providers;         (NEW — this task)
    ├── ProviderKind, ProviderConfig, FakeConfig, FactoryError, factory()
    └── #[cfg(feature = "mlx-runtime")]
        pub mod mlx;           (relocated from src/mlx.rs)
            └── MlxConfig, MlxWhisperSubprocess, parse_script_output, build_segments
                + 10 carry-forward tests
```

### Carry-forward import resolution (helper-daemon's view)

`crates/helper-daemon/src/bridge.rs` line 17:
```rust
use stt_mlx::{SttError, SttProvider};
```
Resolves via `stt-mlx/src/lib.rs` line 24:
```rust
pub use provider::{FakeStt, SttError, SttProvider, SttSegment};
```
→ ✅ resolves to `crates/stt-mlx/src/provider.rs` symbols (same as Phase 2).

`crates/helper-daemon/src/bridge.rs` line 197:
```rust
use stt_mlx::FakeStt;
```
→ ✅ same re-export, same path.

### `include_str!` path

`crates/stt-mlx/src/providers/mlx.rs:36`:
```rust
const TRANSCRIBE_PY: &str = include_str!("../../scripts/transcribe.py");
```
File depth: `crates/stt-mlx/src/providers/mlx.rs` → `..` = `src/providers/` → `..` = `src/` — wait, `include_str!` is relative to the current source file's **directory**. From `src/providers/mlx.rs`:
- `..` = `src/providers/` (the directory containing `mlx.rs`'s parent dir).

Correction: `include_str!` is relative to the **directory** of the file containing the macro invocation. `mlx.rs` lives in `crates/stt-mlx/src/providers/`. So `../../scripts/transcribe.py` = `crates/stt-mlx/src/providers/../../scripts/transcribe.py` = `crates/stt-mlx/scripts/transcribe.py`. ✅ matches the existing script location (`ls crates/stt-mlx/scripts/transcribe.py` confirms).

### Match-arm exhaustiveness (factory)

| Build config | (Mlx, Mlx) | (Mlx, Fake) | (Fake, Mlx) | (Fake, Fake) | Verdict |
|---|---|---|---|---|---|
| `mlx-runtime` ON  | arm 1 (succeed) | catch-all → ConfigMismatch | catch-all → ConfigMismatch | arm 2 (succeed) | ✅ exhaustive |
| `mlx-runtime` OFF | n/a (Mlx variant of ProviderConfig dead-stripped) | `(Mlx, _)` → ProviderDisabled | n/a | arm 2 (succeed) | ✅ exhaustive |

The catch-all `(k, _) => ConfigMismatch` is `#[cfg(feature = "mlx-runtime")]`-gated — under OFF the two preceding arms (`(Fake, Fake)` + `(Mlx, _)` ProviderDisabled) cover all reachable inputs, so no `unreachable_patterns` warning is emitted under either config.

### Trait-object send-safety carry-forward

`crate::provider::tests::sttprovider_is_object_safe_and_send` already verifies that `Box<dyn SttProvider>` compiles + is `Send` (Phase 1 T-1.5). The new `factory_output_is_send_and_swappable_under_mutex` test extends that proof to the factory output specifically, by stuffing the boxed value into `Mutex<Box<dyn SttProvider>>` (which has `Send` as a private bound on `Mutex<T>` but inherited via `T: Send`). This is the runtime hot-swap shape helper-daemon T-3.1 uses.

### Re-verify steps for a host with cargo installed

1. `cd /Users/hieutran/projects/bridge-bot-ts-1/meeting-copilot`
2. `cargo build --workspace` — must compile cleanly.
3. `cargo build --workspace --no-default-features` — must compile cleanly (helper-daemon's `default-features = false` mode of `stt-mlx`).
4. `cargo test --workspace` — must report all carry-forward Phase 1/2 tests + 8 new factory tests pass. Expect:
   - `crate::provider::tests` 8 tests pass
   - `crate::providers::mlx::tests` 10 tests pass (same 10 as Phase 1 — file moved)
   - `crate::providers::tests` 8 new tests pass
   - `helper_daemon::bridge::tests` 5 tests pass (unchanged)
   - `helper_daemon::repo::tests` + `helper_daemon::context::tests` unchanged
   - `audio_capture::*` unchanged
5. `cargo test -p stt-mlx --no-default-features` — must compile + pass (gates the `mlx-runtime`-off path: 8 (provider) + 8 (factory) − 1 (mlx-only test) − 10 (mlx::tests dropped) = 15 tests expected).

A copy of these commands lives in `docs/tasks/phase-3/PHASE-BROWSER-TEST.md` (T-3.10) for the live-host re-verify pass.

## Self-Review Checklist

- [x] **Tests cover happy + edge case** — 8 factory tests cover: kind→str stability, default + override config, kind/config mismatch (both feature configs), byte-parity, Phase 2 seam preservation, `Send`-safe hot-swap, and MLX construction.
- [x] **AC measured numerically** — see metrics table above. Hardware-verify pending; rest pass by inspection.
- [x] **No regression Phase 1/2** — public surface preserved (5 names added, 0 removed); helper-daemon imports unchanged; `include_str!` path repointed and traced; match-arm exhaustiveness verified for both feature configs.
- [x] **Cost tracking** — N/A for T-3.1 (zero LLM calls, zero network calls, refactor only).
- [x] **No secret leak** — no API keys, no env-var reads added (the only env reads — `MLX_WHISPER_PYTHON`, `MLX_WHISPER_MODEL` — are pre-existing in `MlxConfig::default()`, untouched by this task).
- [x] **Background-worker cleanup** — N/A. The `factory_mlx_constructs_subprocess_struct` test's `work_dir` is removed via `std::fs::remove_dir_all` at the end (mirrors Phase 1 T-1.5 test pattern).
- [x] **No `git push`** — per loop rule 4. Commit lands on `main` locally only.
- [x] **No new dependencies** — `Cargo.toml` files unchanged. `thiserror` (already a dep) used for `FactoryError`.

## Risk Re-Check

| From task spec | Outcome |
|---|---|
| **R-1**: Refactor breaks Phase 1/2 cargo tests | Mitigated by relocation-only scope + carry-forward `pub use` re-exports at `lib.rs`. Static-trace shows public symbols + import paths intact. **Hardware re-verify pending** on a host with cargo. |
| **`include_str!` path drift** | Repointed `../scripts/transcribe.py` → `../../scripts/transcribe.py`. Manually verified target file (`crates/stt-mlx/scripts/transcribe.py`) exists. The embedded byte-string is byte-identical to Phase 1's. |
| **Cargo unavailable on sandbox** | Same blocked-action #3 carry-forward; static-trace recorded above; live re-verify documented for T-3.10's `PHASE-BROWSER-TEST.md`. |
| **Future Deepgram / ElevenLabs variant additions** | Designed-for. Adding `ProviderKind::Deepgram` + `ProviderConfig::Deepgram(...)` in T-3.2 is a 2-line additive change; the existing `(k, _)` catch-all returns `ConfigMismatch` for every new mismatched cell without forcing T-3.2 to re-write the factory body. |

## Phase-Goal Re-Check

The Phase 3 exit criterion is "switch provider runtime without restarting; ≥ 2 STT providers stable in 30-min stress test." T-3.1 ships the **runtime hot-swap primitive**: a `Box<dyn SttProvider>` returned by `factory()` that callers (helper-daemon T-3.1, src-tauri commands T-3.6) hold behind `Mutex<…>` and replace with a new `factory()` result. The hot-swap *mechanism* is in place; T-3.2 + T-3.4 add the *destinations* (Deepgram, ElevenLabs).

## Hardware-Verification Caveats

`cargo` is not installed on the loop sandbox (carry-forward of Phase 1 blocked-action #3 — same caveat already documented in `T-1.5-review.md`). T-3.1 ships **code-complete + statically-verified** with the re-verify steps above. Live re-verify on a `cargo`-equipped host is folded into the Phase 3 sign-off (`PHASE-BROWSER-TEST.md`, T-3.10).

The MLX integration test (`mlx_subprocess_transcribes_short_utterance`) carries forward unchanged — still soft-skips when `mlx_whisper` Python module / macOS `say` / `ffmpeg` aren't available, so it's safe to run on any cargo-equipped host even without MLX installed.

## Sign-Off

T-3.1 **code-complete + AC-verified by static trace**. Public surface byte-identical, factory seam in place, 8 new tests added. Live cargo re-verify pending the host-equipped pass. Ready to commit (type `refactor`). Next loop iteration: T-3.2 (Deepgram WebSocket adapter).
