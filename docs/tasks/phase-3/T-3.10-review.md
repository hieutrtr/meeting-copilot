# T-3.10 — Code Review (docs: ARCHITECTURE Phase 3 addendum + provider-comparison.md)

> Reviewer: loop iter 6 self-review against `docs/tasks/phase-3/T-3.10-docs.md`.
> Pre-T-3.10 vitest baseline: **602** (T-3.9 sign-off). Post: **602 / 602** (no test deltas — docs-only task).
> Cargo: untouched (no Rust changes).

---

## What shipped

| Path | Change | Notes |
|---|---|---|
| `docs/ARCHITECTURE.md` | extended (append-only) | 5 new "Phase 3 update" subsections appended after the relevant existing sections: §3.4 (provider trait + factory), §4.1 (TTS feature flag), §11.1 (privacy mode constraint), §14 NEW (telemetry), §15 NEW (cost meter). Existing §1–§13 + Appendix A/B byte-identical. |
| `docs/provider-comparison.md` | new | Per-provider reference doc — STT comparison table, TTS comparison table, per-provider notes, privacy posture matrix, cost reference, setup steps, API maturity caveats, references. |
| `docs/tasks/phase-3/T-3.10-docs.md` | new | Task spec. |
| `docs/tasks/phase-3/T-3.10-review.md` | new | This file. |
| `docs/tasks/phase-3/INDEX.md` | edit | T-3.10 checkbox flipped + outcome line. |

---

## Append-only verification (R-3.10-3)

Goal: prove no existing ARCH content was removed or moved.

```
$ git diff --stat HEAD~0 -- docs/ARCHITECTURE.md
 docs/ARCHITECTURE.md | 162 ++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 162 insertions(+)

$ git diff HEAD -- docs/ARCHITECTURE.md | grep -E "^-[^-]"
(empty — no deletion lines)
```

162 net insertions, 0 deletions. Existing §1–§13 + Appendix A/B byte-identical. The append-only guarantee from `T-3.10-docs.md` §"Non-goals" holds.

---

## Link audit

Every implementation file path mentioned in either ARCH §3.4/§4.1/§11.1/§14/§15 or `provider-comparison.md` was verified to exist on disk this iter:

- `crates/stt-mlx/src/providers/{mod,mlx,deepgram,elevenlabs,backoff,privacy}.rs` — all present.
- `crates/stt-mlx/src/tts/{provider,elevenlabs}.rs` — all present (cargo feature-gated, but the source files exist on disk regardless of feature flag).
- `src/privacy/privacyMode.ts` (+ test) — present.
- `src/tts/{ttsProvider,elevenLabsTts,featureFlag}.ts` (+ tests) — present.
- `src/cost/{sttCostMeter,attachSttCostMeter}.ts` — present.
- `src/llm/sttPricing.ts` (+ test) — present.
- `src/store/settingsStore.ts` — present.
- `src/components/{SettingsSheet,CloudConsentBanner,AnswerPanel}.tsx` (+ tests) — present.
- `src/telemetry/{scrub,sinks,telemetryLog}.ts` (+ tests) — present.
- `src/settings/testConnection.ts` — present.
- `docs/{ARCHITECTURE,IMPLEMENTATION-PLAN,PRD,provider-comparison}.md` — present.
- `docs/tasks/phase-3/INDEX.md` + `T-3.{1..9}-review.md` — present.

No broken links. The link-check ran via a hand-rolled `for f in …; do [ -e "$f" ]; done` loop over the full set of paths cited in either doc.

---

## Review checklist (from spec §"Definition of done")

- [x] **`docs/ARCHITECTURE.md` has 5 new "Phase 3 update" subsections.** §3.4, §4.1, §11.1, §14 (NEW), §15 (NEW). Headers match the spec's "Phase 3 update" pattern.
- [x] **Existing §1–§13 byte-identical.** Verified by `git diff HEAD -- docs/ARCHITECTURE.md` showing 0 `^-` lines (insertions only).
- [x] **`docs/provider-comparison.md` exists with all 9 required sections.** Header + scope (×2 paragraphs) / STT comparison table / TTS comparison table / Per-provider notes (×6: MLX + Deepgram + ElevenLabs Scribe + ElevenLabs TTS + future + decision tree) / Privacy posture matrix / Cost reference / Setup steps / API maturity caveats / References.
- [x] **Both docs cross-reference implementation file paths verbatim (no broken links).** Audit above.
- [x] **`T-3.10-review.md` documents what landed + the link-audit verdict.** This file.
- [x] **`INDEX.md` flips T-3.10 checkbox + outcome line.** Done in same commit.
- [x] **Per-task commit type `docs:` with the co-author footer.** Land alongside this doc.
- [x] **No `git push`.** Local commit only.
- [x] **`bun run test` carry-forward green (602 / 602).** Post-edit run confirms 38 files / 602 tests pass; no flake / no skip.
- [x] **`tsc --noEmit` clean.** `bun run typecheck` returns 0.

---

## No-regression evidence

| Surface | Pre | Post | Delta | Notes |
|---|---|---|---|---|
| vitest total | 602 | 602 | 0 | Docs-only — no source touched. |
| `bun run typecheck` | clean | clean | — | `tsc --noEmit` returns 0. |
| Rust workspace | untouched | untouched | — | `crates/` not in this iter's diff. |
| ARCH §1–§13 | as-of T-3.9 | byte-identical | — | Append-only verified above. |
| ARCH appendices A/B | as-of T-3.9 | byte-identical | — | The new §14/§15 land **before** Appendix A — order preserved. |

---

## Privacy / security review checklist

- [x] **No regression Phase 1/2.** All 602 carry-forward vitest cases still pass; Rust crates untouched.
- [x] **API key never logged.** Docs cite env-var names (`DEEPGRAM_API_KEY`, `ELEVENLABS_API_KEY`) but never key values; no example payloads contain a synthetic key string.
- [x] **Privacy mode constraint enforced at backend, not just UI.** The §11.1 addendum makes the backend-mirror invariant explicit; cross-references `crates/stt-mlx/src/providers/privacy.rs` + `factory_with_privacy`.
- [x] **Telemetry log does NOT contain transcript / audio content.** §14.3 documents the `FORBIDDEN_KEYS` allow-list verbatim from `src/telemetry/scrub.ts`.
- [x] **Source-of-truth rule honored.** Every numeric in the docs (cost rate, latency target, max bytes, retry attempts) cites the implementation file/line where the constant lives; the docs do not invent fresh values.
- [x] **No `git push`.** Local commit only.
- [x] **No comments rot.** Docs are themselves the comment surface; the source-of-truth rule means future drift surfaces in code review (the file path stops resolving, or the numeric mismatches the constant).

---

## Risks revisited (against spec §"Risk + mitigation")

| # | Risk | Status |
|---|---|---|
| R-3.10-1 | Doc drift — addendum claims a feature shipped that hasn't | Mitigated: every "Phase 3.x deferred" phrase is explicit + dated; the §"Out of scope for Phase 3" lines in §11.1 + §14.5 enumerate deferrals. |
| R-3.10-2 | Cross-reference rot | Mitigated this iter via the link audit above. Future grep gate (`bun run docs:link-check`) is a Phase 3.x candidate. |
| R-3.10-3 | Existing ARCH content accidentally deleted | Mitigated: `git diff HEAD -- docs/ARCHITECTURE.md \| grep -E "^-[^-]"` returns empty. Append-only verified. |
| R-3.10-4 | provider-comparison.md duplicates ARCH §13, the two drift | Mitigated: the new doc cites `src/llm/sttPricing.ts` as the canonical source-of-truth and explicitly says "if the docs and the code drift, the code wins". |
| R-3.10-5 | Privacy section over-promises | Mitigated: §11 wording preserved verbatim; §11.1 only describes what changed. The privacy posture matrix in `provider-comparison.md` quotes the §11 mode table verbatim. |

---

## Carry-forward to the phase sign-off

Step 7 of the loop will run the full test suite (`bun run test` + `cargo test --workspace`) and gate Phase 1/2 regression. Step 8 (`PHASE-BROWSER-TEST.md`) writes the live-host verification procedure that exercises the picker switch, privacy banner, cost meter, and telemetry toggle. Step 9 (`PHASE-3-COMPLETE.md`) re-uses the `provider-comparison.md` STT/TTS tables verbatim as the canonical "what shipped" reference + cross-links the ARCH §3.4/§4.1/§11.1/§14/§15 addendum blocks for "where it lives".

The provider-comparison doc's "Selecting a provider — decision tree" section is a candidate to move into the Settings sheet help text in a Phase 3.x UX pass — not in scope this iter.

---

## Commit

```
docs: T-3.10 ARCHITECTURE Phase 3 addendum + provider comparison
```

Includes:

- `docs/ARCHITECTURE.md` (append-only, +162 lines).
- `docs/provider-comparison.md` (new).
- `docs/tasks/phase-3/T-3.10-docs.md` (spec).
- `docs/tasks/phase-3/T-3.10-review.md` (this file).
- `docs/tasks/phase-3/INDEX.md` (T-3.10 checkbox flip + outcome line).
