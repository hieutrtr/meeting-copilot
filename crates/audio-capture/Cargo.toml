[package]
name = "audio-capture"
version = "1.0.0"
description = "Meeting Copilot audio capture — Phase 1 T-1.3 (cpal mic source + WAV fixture source behind AudioSource trait)."
edition = "2021"
rust-version = "1.77"

[lib]
name = "audio_capture"
path = "src/lib.rs"

[dependencies]
cpal = "0.15"
hound = "3.5"
thiserror = "1"
